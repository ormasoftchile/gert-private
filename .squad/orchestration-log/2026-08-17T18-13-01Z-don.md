# Orchestration Log — Don (Backend/Core)
**Date:** 2026-08-17T18:13:01Z
**Task:** Phase 1B Rev 2 Corrections (Auth & Endpoint Safety)

## Work Completed

### Correction A — Managed-Identity vs Workload-Identity Split

- **Verdict:** Correct. Structurally trivial.
- **Feasibility:** One switch case, one map entry in `NewAuthProvider` and `knownAuthProviders`.
- **Revised estimate:** 1.5 days (IMDS only; workload identity deferred to later phase).

### Correction B — Auth and Endpoint Override Safety

#### B-1: TokenGate Verification
- **Finding:** TokenGate exists and is fully implemented at `internal/tool/auth_gate.go:23`.
- **Validation:** `AttachToken` at line 82 checks host before acquiring token.
- **Status:** MCP-012 and MCP-013 guards intact.

#### B-2: allowed_hosts Enforcement
- **Finding:** `allowed_hosts` is doubly enforced, not decorative.
- **Path 1 (Parse time):** `ValidateTransportConfig` (`validate_transport.go:60–74`) enforces MCP-010/011.
- **Path 2 (Runtime):** `TokenGate.AttachToken` (`auth_gate.go:82–91`) blocks every request not in allowed list.
- **Status:** First field in this project found to be genuinely enforced rather than parsed-but-dead.

#### B-3: Tier 0 Endpoint Check
- **Seam:** `checkToolEnvironmentPreflight` in `internal/planner/preflight.go:89`.
- **Design:** Validates profile endpoint override against `allowed_hosts` before execution.
- **Data available:** Profile and tool definition both present at call site.
- **Work:** +0.5 days (PLAN-013 check, folded into Item 2).

#### B-4: Profile Auth Clobbering
- **Finding:** `RuntimeProfile` has no top-level `auth` field — structurally impossible today.
- **Design requirement:** Future Item 2 must allow `provider` only, never `scope`/`allowed_hosts`.

#### B-5: Six Conditions Summary
| Condition | Status | Work |
|-----------|--------|------|
| 1. Transport is mcp-http | Already enforced | 0 |
| 2. Provider explicitly named | Already enforced | 0 |
| 3. Scope explicitly defined | Already enforced | 0 |
| 4. Endpoint host in allowed_hosts | Not yet (PLAN-013) | 0.5 days |
| 5. Tier 0 preflight check | Same as 4 | included |
| 6. TokenGate validates before token | Already satisfied | 0 |

## Conclusion

All corrections verified against code. Four of six endpoint-safety conditions already satisfied. Only PLAN-013 (Tier 0 preflight for endpoint override) is new work, +0.5 days into Item 2.
