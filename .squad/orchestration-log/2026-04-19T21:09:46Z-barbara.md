# Orchestration Log — Barbara (Integrations Specialist)

**Timestamp:** 2026-04-19T21:09:46Z  
**Spawn:** barbara-correctness-strategy  
**Mode:** background  
**Outcome:** success  

## Work Summary

Barbara defined the gert v2 **Correctness/Verification Strategy**, establishing how spec compliance is enforced through testing at every phase.

### Key Deliverables

1. **Spec as Law:** Every normative statement (MUST/MUST NOT/SHALL) maps to a tagged test case with `// spec:` comments linking to source rules.

2. **Test Types by Phase:** Defined test mix for each phase (Unit → Contract → Integration → Golden Trace → Acceptance Corpus).

3. **Spec Compliance Tool:** `gert dev spec-coverage` extracts all MUST rules and cross-references test tags, enforcing ≥95% coverage by Phase 13.

4. **Golden Traces:** JSONL-based deterministic trace comparison with normalized timestamps/IDs and HMAC chaining (Phase 11+).

5. **Phase Gates:** Each phase requires tests passing, spec compliance checklist, ≥80% coverage, and Ken's architectural review.

6. **Brian's TDD Workflow:** Read spec → extract MUST rules → write failing tests → implement → pass → refactor → submit to Ken.

7. **Ken as Spec Reviewer:** Ken verifies spec alignment, interface contracts, dependency rules, test coverage, and completeness before approving each phase.

### Spec References

- 06-runtime-events.md (event ordering, terminal events)
- 12-evidence-tracing-resumption.md (trace structure, HMAC chain)
- 13-error-handling.md (structured errors, error codes)
- 02-architecture.md (dependency graph, layering rules)

### Phase 0 Deliverable

- `internal/dev/speccoverage/` package  
- `gert dev spec-coverage` subcommand  
- CI gate (warning mode until Phase 13)

---

**Status:** ✅ Complete and ready for review.
