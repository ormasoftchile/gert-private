# Orchestration Log: Don — 2026-08-17T14:30:00Z

**Agent:** Don (Backend Dev, claude-sonnet-4.6)  
**Phase:** Runtime Portability Design Verification + Slice 1 Schema Implementation  
**Status:** SHIPPED

---

## Session Scope

Two distinct efforts across negotiations rounds 5–6 and first implementation slice:

### (a) Tri-State Defect Verification and Self-Correction (Round 5 Dialogue)

Verified the counterparty's tri-state defect: `ToolGovernance.RequiresApproval` is a plain `bool` with no field-presence tracking mechanism. A non-nil `*ToolGovernance` proves only that SOME governance property was authored, not that `requires-approval` was written. The published grandfathering rule ("explicit false = legacy read-only signal") was unimplementable.

Corrected own earlier round-2 claim: claimed the engine governance gate fires for plain steps from runbook-level governance. Investigation revealed `GovernanceEvaluator` is **never assigned** in production wiring (`internal/adapter/wire.go`, `pkg/run/run.go`). The pre-flight block is dead for all plain-step invocations today. This is a pre-existing correctness gap, not a regression. Unprompted disclosure to counterparty due to commitment to accuracy.

**Self-Assessments:**
- **Q1–Q6 coverage:** Defect confirmed, blast radius minimal (2-line Go change), runbook-level tri-state over-scoped, no serialization breaks, subprocess sandboxing confirmed as trusted assertion only.
- **Phase 1 Constraint:** Policy must be built from BOTH runbook and per-tool governance using existing OR semantics. Call-site change only; no API change.

### (b) Slice 1: Governance Schema Types (Commit a2e7db0)

Implemented three additive schema changes in `pkg/schema/tool.go`:
- `ToolGovernance.RequiresApproval` `bool` → `*bool` (tri-state: nil/false/true)
- `ToolAction.Classification *string` (values: read-only, mutating, destructive, unspecified)
- `ToolGovernance.AllowedModes []string` (RunMode allowlist, enforcement deferred)

Updated read site: `pkg/pkgsubst/pkgsubst.go:EffectiveGovernanceFromTool()` with nil-guard. `EffectiveGovernance.RequireApproval` remains plain `bool` as agreed — nil resolves to false for resolved type.

Added validation: `internal/tool/scan.go:validateActionClassifications()` rejects unknown classification values.

Added unit tests: `pkg/schema/tool_governance_test.go` covers all four tri-state cases (absent, block-no-field, explicit-false, explicit-true).

**Test Results:**
- `go build ./...` — clean, exit 0
- `go test ./pkg/schema/... ./pkg/pkgsubst/...` — all pass
- `go test ./...` — all pass; conformance corpus (25 requires-approval fixtures) unmarshal correctly

**No conformance fixture edits required.** The `tv-enum.yaml` values unmarshal to `*bool(&false)` correctly; they remain valid.

---

## Surprises

Pre-analysis was accurate. One minor observation: `pkg/pkgsubst/pkgsubst.go` was among 138 pre-existing uncommitted files, so the edit caused git to record it as a "new file" (not in HEAD). Semantic change only: two-line nil-guard in `EffectiveGovernanceFromTool`. Commit carries full file content due to pre-existing state, but delta is minimal.

---

## What Is NOT Done

- **Fixture migration:** AllowedEnvironments → AllowedModes for 25 conformance entries (deferred, no enforcement today)
- **AllowedModes enforcement:** No runtime wiring (later slice)
- **Classification enforcement:** Schema only; gate wiring is Ken's Phase 1 work (internal/engine)
- **GovernanceEvaluator wiring for plain steps:** Ken's work (wire.go, run.go, engine.go, governance/builder.go)

---

## Closure

Slice 1 shipped and reviewed. Passed all tests, zero conformance breaks. Ready for integration into Phase 1 on main.

---

**End Orchestration Log**
