// Package gcp will implement GERT Capture Path parsing and resolution.
// It resolves source-qualified capture paths against step, HTTP, event, and
// local outputs, then stores scalar or subtree results as core.PJVM values for
// later GXL and GIS use.
package gcp
