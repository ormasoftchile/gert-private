// Package core defines shared runtime contracts for GERT expression evaluation.
// Its first responsibility is the Portable JSON Value Model used consistently by
// GXL expressions, GIS interpolation, and GCP capture path resolution so values
// cannot drift between grammar implementations.
package core
