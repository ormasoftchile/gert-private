# Orchestration Log: Edith (Spec Editor)

**Agent:** Edith — Spec Editor  
**Stream:** B — Expression Language Specification  
**Session Date:** 2026-06-05T00:14:04-04:00  
**Duration:** ~527s (background, Sonnet)  
**Status:** ✅ Delivered

## Summary

Edith produced the normative LaTeX specification section for GXL (GERT Expression Language) by encoding the grammar (gxl.ebnf v1.0.0-draft) into structured prose.

## Deliverable

**File:** `design/gert/sections/03a-expression-language.tex` (LaTeX, normative)

### Structure

1. Informative introduction (evaluation sites, design goals)
2. Normative reference to `gxl.ebnf` as source of truth
3. Lexical structure: whitespace, comments, keywords, identifiers, string literals, number literals, operators, forbidden tokens
4. Syntactic structure: precedence table (8 levels), full grammar, literal forms, GDP path access, function calls, unary/binary operators
5. Standard library: `len`, `str.*`, `list.*`, `regex.*`, `math` (reserved)
6. Semantics: type system (PJVM), short-circuit evaluation, GDP path access rules, arithmetic (division-by-zero, modulo notes)
7. Error class catalog (GXL-PARSE-001..010, GXL-TYPE-001..004, GXL-PATH-001..003, GXL-EVAL-001..004) in a table
8. Informative migration note

## Discrepancies Found (Grammar Authoritative — All Resolved)

| # | Proposal | Grammar | Resolution |
|----|----------|---------|------------|
| DISC-1 | No scientific notation | Scientific notation permitted (`EXP` production) | Grammar wins; spec follows grammar |
| DISC-2 | `len` as namespace | `len` is standalone builtin, not namespace | Grammar wins; spec encodes standalone only |
| DISC-3 | Missing `str.length`, `list.indexOf`, `list.length` | All three defined in grammar | Grammar wins; all three included in spec table |
| DISC-4 | "Member access on identifiers forbidden" | `GDP = IDENT { DOT IDENT \| LBRACKET INTEGER RBRACKET }` is valid | Grammar wins; GDP is explicitly valid in GXL |

**Impact:** Grammar is authoritative source of truth. Proposal to be amended in Stream B Phase 2.

## Semantic Gaps

1. **PJVM Definition Deferred:** Grammar references GERT Portable JSON Value Model (PJVM, defined in gis.ebnf §PJVM). Type table in spec §6.1 constructed from grammar inline descriptions. When Stream B encodes GIS, PJVM must be defined once (likely shared §02 or §03) and cross-referenced.

2. **Conformance Corpus Forward Reference:** Spec references TV-GXL-EVAL category for short-circuit vectors. Phase 1 deliverable owned by Tess (Conformance Tester). No placeholder `\TODO` added; forward reference label sufficient.

3. **Cross-Reference: capture.default:** Spec mentions "capture defaults (GCP grammar)" for optional values. GCP spec section not yet written. Forward reference label added; will be updated when GCP section available.

## LaTeX Technical Notes

1. **File not wired into main.tex yet** — deferred to Stream B integration task to avoid merge conflicts with concurrent work.
2. **\TODO macro** defined locally via `\providecommand` — if promoted to main preamble, local definition silently superseded.
3. **\textcolor{red}** requires `xcolor` (already loaded in main.tex). No additional packages needed.
4. **minted blocks** use `text` lexer for grammar productions (avoids need for GXL-specific Pygments lexer).
5. **Table widths:** Error table (Table 2) uses `p{Ncm}` paragraph columns. If overflow, convert to `longtable` (already loaded).
6. **Label convention:** Uses `\label{sec:gxl}` and `\label{sec:gxl:...}` (not `\label{ch:gxl}`). Cross-references use `\ref{sec:gxl}`.

## Terminology Committed

The following terms chosen and MUST be used consistently going forward:

| Term | Rejected Alternative | Rationale |
|------|---------------------|-----------|
| **evaluator** | "interpreter" | One term for one concept; evaluator is runtime GXL executor |
| **GDP** | "dot path", "path expression" | Grammar's own term; GERT Dotted Path |
| **PJVM** | "value model", "type model" | Grammar's own term; GERT Portable JSON Value Model |
| **parse error** vs. **evaluation error** | "runtime error" | Timing distinction is normative |
| **context map** | "environment", "scope", "variable map" | Grammar's term; runtime variable scope |
| **boolean position** | "boolean context", "boolean expression" | Grammar's term from §5.3 |

## Phase 2 Follow-Up

The following spec sections contain GXL-conflicting content (old `expr-lang/expr` or Go-template syntax). Not modified in Stream B Day 1; queued for Stream B Phase 2 iterations:

| File | Lines (approx.) | Conflict |
|------|-----------------|----------|
| sections/03-schema-vnext.tex | 412–560 | Describes `expr-lang/expr` with `&&`, `||`, `!` operators |
| sections/03-schema-vnext.tex | 2518–2532, 2543–2560 | Branch `condition` still labeled "Go template" |
| sections/03-schema-vnext.tex | 1250–1262, 1333–1354 | `collector.fields[].when` described as Go template |
| sections/02-architecture.tex | 467–477 | CLI step interpolation uses Go `text/template` |
| sections/02-architecture.tex | 586–587, 1385–1412 | `display.content` described as Go template |

## Handoff Notes

- **Spec Editor (Edith) ready for Phase 2:** rewrite sections identified above, update proposal §2.6 and §2.4 per grammar authority
- **Design Authority (Barbara):** Confirm whether DISC-4 (GDP in GXL expressions) is intended or grammar needs revision. Current spec encodes grammar as authoritative.

---

*Orchestration log generated by Scribe at 2026-06-05T00:14:04-04:00*
