# Orchestration: Ken MCP Bridge Engagement

**Timestamp:** 2026-08-19T17:02:17Z  
**Agent:** Ken (Backend Dev)  
**Mode:** background  
**Model:** claude-sonnet-4.6  
**Repo:** C:\One\OpenSource\gert-vscode (commit e395bf8)

## Route Reason

Cristián reported he still cannot use ICM MCP from the gert VS Code extension — the standing engagement blocker. Ken owns the extension MCP bridge work.

## Primary Finding

**Severity: HIGH**

The `/run` chat handler was **completely absent** from `src/extension.ts` after the Petals lifecycle port (commit 742e368). That commit deleted the pump-based `/run` implementation and never added a replacement. The manifest still declared `run`, so VS Code routed the command, but the handler fell through `isArmCommand()` to "Unknown command" for EVERY `@gert /run` since.

**Test coverage gap:** All 169 tests passed because none exercise the extension.ts handler body (requires a VS Code host). This is the FIFTH occurrence of the vacuity defect class on this engagement.

## Fix Applied

**Status:** Unstaged (for Cristián's review)

- Minimal Petals-style `/run` handler
- Parse `<runbook.yaml> [key=val …]` from request.prompt
- `refreshBridgeRegistry(runbookPath)` → spawn `gert run [--var …] <runbook.yaml>`
- Inject GERT_VSCODE_BRIDGE_URL + GERT_VSCODE_BRIDGE_TOKEN
- Errors via reportEngineFailure
- New manifest test INVTOKEN-M-03

**Verification:**
```
npm run compile ✅ 0 errors
npm test ✅ 186/186 pass
```

## Second Finding

**Current user blocker: provider-side, not gert-side.**

VS Code cannot start `icm-mcp` — `icm-mcp-prod.azure-api.net` returns 401 HTTP status. Gert code does not execute until the provider is healthy. Gert's reporting of this is already correct:

- T1 ("MCP server has stopped") → tested in F4
- T2 ("MCP server could not be started: 401 status…") → tested in F4
- Both classify as `provider_unavailable` with operator guidance: "MCP: List Servers" and re-authentication

**No code change needed on the gert side.**

## Honest Status

**The full live path has NEVER ONCE succeeded in live VS Code:**

Full path: @gert /run → gert binary → bridge HTTP → vscode.lm.invokeTool → ICM tool → result

The 186 tests are non-vacuous proofs of bridge logic against mocks but do not imply the live path works.

## Recovery Steps

See Ken's root-cause analysis in `.squad/decisions.md` (merged from inbox) for detailed provider recovery procedure.

## Artifacts

- `.squad/agents/ken/history.md` (appended)
- `.squad/decisions/inbox/ken-icm-mcp-live-blocker.md` (merged to decisions.md, deleted)
