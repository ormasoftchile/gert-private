# Orchestration Log: ken-13

**Session:** 2026-08-19T02-05-00Z  
**Agent:** Ken (VS Code Extension Developer)  
**Mode:** Sync then multi-turn (claude-sonnet-4.6)  
**Topic:** Chat-mediated MCP execution architecture

---

## Objective

Replace the cached-token bridge architecture with chat-mediated MCP execution so that `vscode.lm.invokeTool()` is called from within the active chat handler's execution context rather than from a loopback HTTP handler after the chat request has returned.

---

## Round 1 → commit 9dfd345

### Deliverables

- `src/runPump.ts` — `RunPump` class: queue of `PendingInvocation` items drained by the handler's concurrent pump-processor
- `src/runClient.ts` — bridge adapter: enqueues invocations into the pump instead of calling `invokeTool` directly
- `src/runLoop.ts` — `Promise.race([terminal, cancel, deadline])` loop holding the handler open

### Key architectural decisions

- `@gert /run` chat command holds the handler open until terminal state, cancellation, or deadline
- In-handler invocation pump: `vscode.lm.invokeTool()` called only from live handler context
- Petals-derived two-attempt fallback: retry with `token=undefined` on `/\bCanceled\b/` failure
- Pre-invoke token gate replaced by `no_active_run` gate (`hasActivePump?.() === false`)
- `/arm-mcp` demoted to diagnostic only

### Test results

**161/161/0/0** — worktree `C:\One\OpenSource\_ken_v7` at 9dfd345.

### Coordinator rejection items

**PUMP-8 vacuous (third occurrence of wrong-layer mutation).**  
PUMP-8 forced attempt 1 to throw `Canceled`, exercising only the retry path. A token leak injected on the attempt-1-success path produced 161/161/0/0. The "non-vacuity control" canary proved the scanner's `includes()` works on a crafted string — not that the scanner sees the leaking lines. Rejected.

**Deadlock is not a test failure.**  
Mutation #7 (remove `pump.close()`) caused an infinite hang. No timeout was set. "Suite hangs — kill" is not a named failure and not a non-zero exit. No evidence. Rejected.

---

## Round 2 → commit 4560d82

### Changes from Round 1

- **PUMP-8 split into PUMP-8a/8b/8c/8d** — one sub-test per path through `invokeWithTwoAttempts`: attempt-1 success, non-Canceled failure, Canceled retry success, Canceled retry failure. Each confirmed as 163/164 kill before acceptance.
- **`--test-timeout=5000` added** to npm test. Slowest legitimate test measured 95.8ms; 5000ms = ~52× headroom. Mutation #7 now fails PUMP-5/6/7 at 5001–5006ms, wall clock 18.8s, exit 1.
- **`extractTestGlobs` regex updated** in `repoBoundary` to skip flags between `--test` and the glob: `/node\s+--test(?:\s+--[\w][\w=-]*)*\s+<glob>/`. Prevents `repoBoundary/rule5` regression.

### Test results

**164/164/0/0**

---

## Coordinator Independent Verification

Detached worktree, `git status --porcelain` empty, `npm ci` clean, 164/164/0/0.

Verification probes run independently:
- Token leak on attempt-1 success path (new in 4560d82) → **fails PUMP-8a** ✓
- Glob narrowed to prove regex change did not weaken guard → **repoBoundary/rule5 fails** ✓
- `pump.close()` removed from finally → **PUMP-5/6/7 fail at 5s boundary**, wall clock 18.6s, exit 1 ✓

**ACCEPTED.**

---

## Repository State at Close

| Repo | HEAD |
|------|------|
| gert-vscode | 4560d82 (pushed) |
| gert | 18093a5 |
| gert-private | 286121f (pushed) |

---

## Decisions Recorded

- Chat-mediated MCP execution architecture (commits 9dfd345 / 4560d82)
- Mutation evidence requires bounded named failure (`--test-timeout=5000`)
- Vacuity in redaction proofs — third occurrence; PUMP-8a/8b/8c/8d as the corrective pattern
