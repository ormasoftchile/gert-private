# Orchestration Log — David (Blocker Revision & PKG-017)
**Session:** 2026-08-09T19:33:43-07:00  
**Agent:** David (Tool Packages Revisions)  
**Role:** Independent revision of all blockers; implementation of PKG-017 sanction

## Summary
David was selected for independent revision of all five blockers (B1–B5) after Barbara's first-pass gate lockout of Don and Ken. David executed a complete revision of blocker items, discovering and fixing a latent defect, and choosing the sanctioned fail-closed PKG-017 approach for §5 include-closure handling.

## Blockers Resolved

### B1 — Plan-Time Substitution Validation
- Added `flowwalk.Walker`-based visitor in `cmd/gert/substitution_plan.go`
- Invoked from `runWithMode` unconditionally, before `plannerImpl.Plan` and `eng.Start`
- `dryrun` mode automatically covered by construction
- Eagerly walks branch arms, iterate/parallel/compensate bodies; no lazy expansion
- `pkgsubst.Plan` per frame with accumulated `[]pkgsubst.Frame`; cycle/depth decided statically
- Tests: `TestRun_Substitution_PlanTimeContractViolation_DryRun`, `..._UnreachableStep` verified live

### B2 — Digest Closure
- `pkgcatalog.closureDigestLines` / `closeIncludeFile` resolve export `execute.path` via `ResolveKind(KindExecutePath)`
- Recurse through package-internal includes via `KindPackageInternalInclude`
- Cycle-guarded by shared `closureVisited` set; actions iterated sorted
- `digestLines` sorted before hashing
- Include extraction via raw `*yaml.Node` walk (collides on inline tags in typed decode)
- Closure files hashed as `hex relpath_posix_nfc`; files outside closure unhashed

### B3 — Catalog Digest from Package Digest
- Export entries deferred through `pending` slice; added only after `pkgDigest` computed
- `Entry.Digest = pkgDigest` for tier-1; tiers 0/2/4 keep file digests
- `CatalogDigest()` now proves §7.3 guarantee
- Test: `TestBuild_DigestClosure_SubstituteRunbookByteChange_ChangesPackageDigest` verifies property

### B4 — Removed/Added Package on Resume
- `checkResumePackageDrift` treats `Plan.Metadata.PackageDigests` as authoritative
- Missing recorded package → `PKG-001` (before digest comparison)
- Resolved package absent from manifest → `PKG-009` forced drift pair
- Sorted pairs for stable diagnostics
- Tests: `TestCheckResumePackageDrift_RemovedPackage_PKG001`, `..._AddedPackage_PKG009`

### B5 — `outputs.<name>` Capture Root
- `gdp.SourceOutputs` added to grammar
- `pkg/gcp/parser` accepts `outputs.<name>` and rejects GDP suffix (`GCP-PARSE-002`)
- `pkg/capture.resolveOutputs` resolves against current step's `Output` map only
- `GCP-RESOLVE-002` for unknown name
- No `step.*.outputs.*` form added
- Plan validation in `internal/planner/validate.go:stepBindsSubstitutionAction` (`GCP-PARSE-001`)

### Latent Defect Found and Fixed
`schemaToolDefFromRuntime` was dropping `Execute` and `Outputs` fields, causing every catalog-bound substitution action to read as a plain action to the planner. This would have silently defeated both B1 and B5. David found and fixed this defect independently.

## Nine §3 Required Fixes (All Verified)
1. `ConstraintSources` ordered real  
2. PKG-002 provenance (both catalog.go and bind.go)  
3. Build metadata in constraints (+ added)  
4. PKG-006 ordering (sorted, deterministic)  
5. PLAN-010 typed error (errkit.ErrPLAN010)  
6. PKG-018 normalisation (case/NFC comparison)  
7. `maxLinkHops` explicit count (Windows verified manually)  
8. Output coercion (step fails on mismatch)  
9. `--package-map` provenance (`origin` recorded)  

## §5 Include-Closure: PKG-017 Sanction
David chose the explicitly sanctioned fallback (b) for §5 include-closure semantics:
- `cmd/gert/include_closure.go` walks entire closure eagerly
- Fails closed with typed `PKG-017` for any *included* runbook declaring `requires:` or `toolRefs:`
- Message names the file and states remedy
- Option (a) full per-file lexical binding deferred; recorded as follow-up (not silent gap)
- Both §5 tests pass

## Code Quality & Hygiene
- ✅ Protected files byte-identical to pre-revision state (verified by mtime)
- ✅ No spec modifications (design/ unchanged during revision window 18:20–19:20)
- ✅ Nothing staged or committed; dirty tree preserved
- ✅ `go build ./...` clean
- ✅ `go test ./...` green except pre-existing flaky `TestSSE_FilterByRunID`
- ✅ Target suites green: `internal/planner`, `pkg/pkgsubst`, `pkg/gcp/parser`, `pkg/capture`, `internal/conformance`

## Honest Scope Reporting
David explicitly flagged scope reduction on §5 (deferred option (a)) and honestly reported Windows-skipped symlink tests — precisely the transparency that enabled Barbara to verify those items herself.

## Status
✅ **COMPLETE** — All blockers resolved; latent defect fixed; PKG-017 sanction honoured; Barbara's final gate approval achieved.

---
**Co-authored-by:** Copilot <223556219+Copilot@users.noreply.github.com>
