# Orchestration Log: ken-12

**Timestamp:** 2026-08-19T00:46:40Z  
**Agent:** ken-12  
**Task:** Add pretest lifecycle hook to npm test  
**Commit:** f667f0a  

## Execution Summary

Sync task: Added `"pretest": "npm run compile"` to package.json in gert-vscode to ensure build artifacts are regenerated before each test run.

## Key Results

- **Verification environment:** Clean detached worktree, no out/ directory
- **Pre-fix test results:** npm ci → npm test alone → 153/153/0/0
- **Mutation direction 1 (forward):** Mutate `isArmCommand` → npm test without manual recompile → 152/1 fail (INVTOKEN-5) ✓
- **Mutation direction 2 (revert):** Revert mutation → npm test without manual recompile → 153/153/0/0 (stale out/ causes false red without fix)
- **Post-fix verification:** Both directions correct after pretest hook deployed

## Deliverables

- npm pretest hook added to package.json
- No change to test script shape (rules 5 and 6 continue to parse identically)
- Binding rule documented: stale build artifacts invalidate mutation testing (BUG CLASS #13)

## Mutation Testing Verification

| Scenario | Before Pretest | After Pretest | Result |
|----------|---|---|---|
| Fresh worktree, `npm test` | 153/153 | 153/153 | ✓ Correct |
| Mutate + `npm test` (no manual compile) | 153/0 (false green) | 152/1 (caught) | ✓ Fixed |
| Revert + `npm test` (no manual compile) | 152/1 (false red) | 153/153 (correct) | ✓ Fixed |

## Impact on CI

CI pipeline (npm ci → compile → test → package) already correct. Adds one redundant tsc pass before the already-compiled test step — acceptable tradeoff.

## Systemic Bug Class #13

This commit repairs the 13th distinct bug class in this engagement: stale or untracked build artifacts causing spurious test failures. The binding rule is documented in decisions.md and applies to all verification work.

## Status

✅ ACCEPTED. Deployed to gert-vscode main branch.
