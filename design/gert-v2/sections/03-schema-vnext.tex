% ============================================================
% §03 — Schema vNext
% Author: John (YAML/Schema Specialist)
% Last updated: 2026-04-18
% ============================================================

\chapter{Schema vNext}

\label{ch:schema}

This chapter is the normative specification for all gert v2 data structures:
runbook documents, tool definitions, and provider definitions.
It supersedes all v1 schema documentation and defines the contracts that Brian's
parser, Ken's runtime, and the VS~Code extension must implement.

The design philosophy is \emph{minimal core, extensible periphery}: the core
language fields that the runtime must understand are small, typed, and stable;
everything else is declared as an extension with an explicit namespace.  Where
v1 accumulated surface area organically, v2 draws a hard boundary and enforces
it at parse time.

% ============================================================
\section{Schema Identity and Versioning}
\label{sec:schema-identity}
% ============================================================

\subsection{apiVersion Values}
\label{subsec:apiversion}

Every gert document carries a mandatory \texttt{apiVersion} field that
identifies both the document kind and the schema version.  The v2 values are:

\begin{center}
\begin{tabular}{lll}
\hline
\textbf{Document kind} & \textbf{v1 value} & \textbf{v2 value} \\
\hline
Runbook                & \texttt{runbook/v1} & \texttt{runbook/v2} \\
Tool definition        & \texttt{tool/v0}    & \texttt{tool/v2}   \\
Provider definition    & \texttt{provider/v0}& \texttt{provider/v2} \\
\hline
\end{tabular}
\end{center}

The format is \texttt{<kind>/<major>}.  Minor and patch changes do not alter
the \texttt{apiVersion} string; they are tracked by the schema artifact version
(see \S\ref{sec:schema-artifacts}).

\subsection{Self-Description via \$schema}
\label{subsec:self-description}

Following JSON~Schema~\cite{jsonschema-spec} best practice and the pattern used by GitHub Actions,
Argo~Workflows, and Kubernetes~CRDs, every v2 document \emph{should} carry a
\texttt{\$schema} field pointing to the canonical JSON~Schema URL:

\begin{minted}{yaml}
$schema: "https://schemas.gert.dev/runbook/v2.json"
apiVersion: runbook/v2
id: service-health
name: Service Health Check
...
\end{minted}

The \texttt{\$schema} field is optional at runtime (the runtime uses
\texttt{apiVersion} for dispatch) but is \emph{strongly recommended} because:

\begin{itemize}
  \item VS~Code and JetBrains IDEs provide schema-backed auto-completion and
        inline validation when \texttt{\$schema} is present.
  \item The schema URL carries an implicit version contract: updating the URL
        updates the validation rules visible to tooling.
  \item It enables offline validation with any JSON~Schema Draft~2020-12
        validator (ajv, jsonschema, check-jsonschema).
\end{itemize}

Schema URLs for each document kind:

\begin{verbatim}
https://schemas.gert.dev/runbook/v2.json
https://schemas.gert.dev/tool/v2.json
https://schemas.gert.dev/provider/v2.json
\end{verbatim}

\subsection{Versioning Strategy}
\label{subsec:versioning-strategy}

gert schema versioning follows a simplified semantic versioning model applied
to the \texttt{apiVersion} major component:

\begin{itemize}
  \item \textbf{Major increment} (e.g.\ \texttt{runbook/v1} → \texttt{runbook/v2}):
        A breaking change.  The parser \emph{must not} silently accept the old
        format.  Automated migration is provided via \texttt{gert migrate}.
  \item \textbf{Minor/patch changes within a major}: Additive, backward-compatible.
        New optional fields may be added.  Existing fields may not be removed or
        renamed.  The JSON~Schema artifact version tracks these with a semver
        suffix (e.g.\ \texttt{runbook/v2.json} at artifact version
        \texttt{2.1.0}).
\end{itemize}

\paragraph{What constitutes a breaking change}

A breaking change is any of:
\begin{itemize}
  \item Renaming or removing a required field.
  \item Narrowing an existing enum (removing a valid value).
  \item Changing a field type (e.g.\ string → object).
  \item Changing the meaning of a field such that existing runbooks produce
        different runtime behavior.
  \item Removing a step type.
\end{itemize}

Adding new optional fields, adding new enum values, and loosening existing
constraints are \emph{not} breaking changes.

\subsection{Compatibility Contract: v1 → v2}
\label{subsec:compatibility-contract}

\begin{center}
\begin{tabular}{llp{6cm}}
\hline
\textbf{v1 field} & \textbf{v2 status} & \textbf{Notes} \\
\hline
\texttt{apiVersion: runbook/v0} & Removed & Handled by \texttt{gert migrate} \\
\texttt{apiVersion: runbook/v1} & Deprecated & Compatibility mode in v2 parser \\
\texttt{meta.name}              & Promoted  & Becomes top-level \texttt{name} \\
\texttt{meta.kind}              & Promoted  & Becomes top-level \texttt{kind} \\
\texttt{meta.description}       & Promoted  & Becomes top-level \texttt{description} \\
\texttt{meta.vars}              & Retained  & Moved to top-level \texttt{vars} \\
\texttt{meta.inputs}            & Retained  & Moved to top-level \texttt{inputs} \\
\texttt{meta.governance}        & Retained  & Moved to top-level \texttt{governance} \\
\texttt{meta.prose}             & Retained  & Moved to top-level \texttt{prose} \\
\texttt{meta.defaults}          & Retained  & Moved to top-level \texttt{defaults} \\
\texttt{imports}                & Retained  & Unchanged \\
\texttt{tools}                  & Replaced  & Replaced by \texttt{toolRefs} (typed) \\
\texttt{flow}                   & Retained  & Canonical execution root \\
\texttt{tree}                   & Removed   & Use \texttt{flow} \\
\texttt{steps}                  & Removed   & Flat step list was never canonical \\
\texttt{step.type: collector}   & Renamed   & Split into \texttt{choice}, \texttt{decision}, \texttt{collector} \\
\texttt{step.type: router}      & Renamed   & Becomes \texttt{type: end} \\
\texttt{step.type: choice}      & New       & User selects from options; stores result \\
\texttt{step.type: decision}    & New       & User picks runbook path; routes execution \\
\texttt{step.type: noop}        & Removed   & Use \texttt{when:} guard on any step \\
\texttt{step.type: extension}   & Retained  & Unchanged \\
\texttt{step.with.argv}         & Deprecated& Use \texttt{type: cli} with \texttt{command:} \\
\texttt{step.assertions}        & Deprecated& Use \texttt{step.type: assert} \\
\hline
\end{tabular}
\end{center}

The v2 parser reads \texttt{runbook/v1} documents in \textbf{compatibility mode}:
all v1 field paths are accepted and silently normalized to their v2 equivalents
before validation.  Running \texttt{gert migrate} permanently rewrites the file
to v2 canonical form.  v2 will not support \texttt{runbook/v0}; documents at
that version must be migrated to v1 first or directly to v2 via a two-stage
migration.

\subsection{\texttt{gert migrate} Behavior}
\label{subsec:migrate}

\begin{minted}{bash}
# Dry-run preview
gert migrate --dry-run runbook.yaml

# In-place rewrite
gert migrate runbook.yaml

# Migrate a whole directory
gert migrate ./runbooks/
\end{minted}

Migration rules applied automatically:
\begin{enumerate}
  \item Rewrite \texttt{apiVersion} to \texttt{runbook/v2}.
  \item Add \texttt{\$schema} field.
  \item Promote \texttt{meta.name}, \texttt{meta.kind}, \texttt{meta.description}
        to top-level.
  \item Move remaining \texttt{meta.*} fields to their v2 top-level equivalents.
  \item Rename \texttt{tree:} to \texttt{flow:}.
  \item Replace generic \texttt{step.type: manual} from early v2 drafts with
        three precise types: \texttt{choice}, \texttt{decision}, \texttt{collector}.
  \item Rename \texttt{step.type: router} → \texttt{end}.
  \item Rewrite \texttt{tools: [name]} shorthand to \texttt{toolRefs:} typed
        declarations.
  \item Add \texttt{id:} to runbook if absent (derived from \texttt{name}).
\end{enumerate}

% ============================================================
\section{Core Runbook Fields (v2)}
\label{sec:core-fields}
% ============================================================

\subsection{Top-Level Field Inventory}
\label{subsec:field-inventory}

The v2 runbook document has the following top-level fields:

\begin{center}
\begin{tabular}{lllp{5.5cm}}
\hline
\textbf{Field} & \textbf{Type} & \textbf{Required} & \textbf{Description} \\
\hline
\texttt{\$schema}     & string  & No  & JSON~Schema URL (recommended) \\
\texttt{apiVersion}   & string  & Yes & Must be \texttt{runbook/v2} \\
\texttt{id}           & string  & Yes & Unique machine identifier (slug) \\
\texttt{name}         & string  & Yes & Human-readable display name \\
\texttt{kind}         & enum    & No  & \texttt{mitigation | reference | composable | rca} \\
\texttt{description}  & string  & No  & Short prose summary \\
\texttt{vars}         & map     & No  & Default variable values \\
\texttt{inputs}       & map     & No  & Input declarations (\S\ref{sec:inputs-outputs}) \\
\texttt{outputs}      & map     & No  & Output declarations (\S\ref{sec:inputs-outputs}) \\
\texttt{toolRefs}     & array   & No  & Tool references (\S\ref{subsec:toolrefs}) \\
\texttt{imports}      & map     & No  & Named runbook imports \\
\texttt{defaults}     & object  & No  & Default step settings \\
\texttt{governance}   & object  & No  & Governance policy \\
\texttt{prose}        & object  & No  & Human-readable TSG sections \\
\texttt{metadata}     & map     & No  & Arbitrary key-value annotations \\
\texttt{flow}         & array   & Yes & Execution tree (TreeNode array) \\
\hline
\end{tabular}
\end{center}

\paragraph{id field}
The \texttt{id} is a slug: lowercase alphanumeric with hyphens, matching
\texttt{\^{}[a-z][a-z0-9-]*[a-z0-9]\$}.  It uniquely identifies a runbook within
a project and is used by the \texttt{include} step and the trace record.  It must
be stable across renames; changing it is a breaking change for any runbook that
includes it by id.

\paragraph{kind values}
\begin{itemize}
  \item \texttt{mitigation} — Incident response procedure; governance defaults
        to \texttt{require-approval} for destructive effects.
  \item \texttt{reference} — Documentation-first runbook; dry-run by default.
  \item \texttt{composable} — Sub-runbook intended for include from other
        runbooks; not directly executable from CLI.
  \item \texttt{rca} — Root-cause analysis template.
\end{itemize}

\subsection{toolRefs}
\label{subsec:toolrefs}

In v1, tools were declared as a flat string array (\texttt{tools: [curl, nslookup]}).
In v2, \texttt{toolRefs} is a typed array that allows aliasing and explicit path
overrides:

\begin{minted}{yaml}
toolRefs:
  - name: curl
  - name: nslookup
  - name: k8s
    path: ./tools/k8s.tool.yaml   # override default discovery
    alias: kubectl-wrapper        # optional local alias
\end{minted}

The default discovery path for \texttt{name: foo} is \texttt{tools/foo.tool.yaml}
relative to the runbook file.  The \texttt{path} field overrides this.
The \texttt{alias} field lets the runbook reference the tool by a local name
that differs from the tool's declared \texttt{meta.name}.

\subsection{Minimal Valid v2 Runbook}
\label{subsec:minimal-runbook}

\begin{minted}{yaml}
$schema: "https://schemas.gert.dev/runbook/v2.json"
apiVersion: runbook/v2
id: hello-world
name: Hello World

flow:
  - step:
      id: greet
      type: cli
      title: Print greeting
      command: echo
      args: ["Hello, world!"]
      capture:
        output: stdout
  - step:
      id: done
      type: end
      title: Complete
      outcome:
        category: resolved
        code: success
\end{minted}

\subsection{Complete Annotated Example}
\label{subsec:full-example}

\begin{verbatim}
$schema: "https://schemas.gert.dev/runbook/v2.json"
apiVersion: runbook/v2
id: service-health-check
name: Service Health Check
kind: mitigation
description: |
  Validate DNS resolution and HTTP endpoint reachability
  for a named service. Escalates on failure.

vars:
  default_timeout: "30s"

inputs:
  hostname:
    type: string
    required: true
    description: "Hostname to check"
    from: prompt
  incident_id:
    type: string
    required: false
    description: "Incident tracking ID"
    from: incident.id       # provider-resolved

outputs:
  health_status:
    type: string
    description: "Final health assessment"
    from: captures.http_result

toolRefs:
  - name: curl
  - name: nslookup

governance:
  rules:
    - effects: ["network.probe"]
      action: allow
    - effects: ["service.restart"]
      action: require-approval
      min_approvers: 2
  redact:
    - pattern: "(?i)Authorization:\\s*\\S+"
      replace: "Authorization: <redacted>"

defaults:
  timeout: "60s"

prose:
  safety: |
    Run this runbook only during an active incident. Do not
    loop — each execution creates a trace record.
  prerequisites: "Requires curl >= 7.80 and nslookup on PATH."

metadata:
  team: platform-sre
  runbook-url: "https://wiki.example.com/runbooks/service-health"
  severity: P2

flow:
  - step:
      id: check_dns
      type: tool
      title: "Resolve DNS: {{ .hostname }}"
      tool:
        name: nslookup
        action: lookup
        args:
          host: "{{ .hostname }}"
      capture:
        dns_result: stdout
      contract:
        effects: ["network.probe"]
        reads: ["hostname"]
        writes: ["dns_result"]

  - step:
      id: check_http
      type: tool
      title: "HTTP health check: {{ .hostname }}"
      tool:
        name: curl
        action: head
        args:
          url: "https://{{ .hostname }}/healthz"
      capture:
        http_result: stdout
      timeout: "{{ .default_timeout }}"

  - step:
      id: evaluate
      type: branch
      title: Evaluate health results
      branches:
        - condition: '{{ contains .http_result "200" }}'
          label: Healthy
          steps:
            - step:
                id: resolved
                type: end
                title: Service is healthy
                outcome:
                  category: resolved
                  code: http_ok
        - condition: '{{ not (contains .http_result "200") }}'
          label: Unhealthy
          steps:
            - step:
                id: escalate
                type: end
                title: Escalate to on-call
                outcome:
                  category: escalated
                  code: http_fail
\end{verbatim}

% ============================================================
\section{Input and Output Declarations}
\label{sec:inputs-outputs}
% ============================================================

\subsection{Input Declarations}
\label{subsec:inputs}

Inputs declare variables that must be resolved before execution begins.  In v2
the \texttt{inputs} map is a top-level field (promoted from \texttt{meta.inputs}).

\begin{minted}{yaml}
inputs:
  <name>:
    type: string | number | boolean | secret
    required: true | false          # default: true
    description: "..."
    default: "..."                  # valid only when required: false
    example: "..."
    pattern: "^[a-z]+"             # regex validation (string only)
    from: <binding>                 # resolution source
\end{minted}

\paragraph{type field}
The \texttt{type} field (new in v2) declares the value's type.  Supported values:

\begin{itemize}
  \item \texttt{string} — Default. UTF-8 text.
  \item \texttt{number} — Integer or float, passed as string in templates.
  \item \texttt{boolean} — \texttt{true} or \texttt{false}.
  \item \texttt{secret} — String, redacted in traces and log output.
\end{itemize}

\paragraph{from: binding syntax}

The \texttt{from} field specifies how the value is resolved.  v2 binding
forms:

\begin{center}
\begin{tabular}{lp{7.5cm}}
\hline
\textbf{Binding} & \textbf{Behavior} \\
\hline
\texttt{prompt}          & Ask the operator interactively at runtime \\
\texttt{env.<NAME>}      & Read from environment variable \texttt{NAME} \\
\texttt{file.<path>}     & Read first line from a local file \\
\texttt{<provider>.<field>} & Delegate to a registered input provider (e.g.\ \texttt{incident.id}) \\
\texttt{var.<name>}      & Copy from another variable or capture \\
\hline
\end{tabular}
\end{center}

If \texttt{from} is omitted, the field must have a \texttt{default} value or the
runtime will reject the runbook at semantic validation time.

\subsection{Output Declarations}
\label{subsec:outputs}

Outputs declare values that a composable runbook exposes to its callers.
With \texttt{type: include}, the caller's variable space is shared directly;
explicit output declarations are reserved for the future \texttt{type: call}
step type (post-v2.0 scope, isolated scope with explicit I/O mapping).

\begin{minted}{yaml}
outputs:
  <name>:
    type: string | number | boolean | secret
    description: "..."
    from: captures.<captureName>    # must reference a capture variable
\end{minted}

The \texttt{from: captures.*} binding is the only supported form in v2.
The named capture must be written by at least one step in the runbook; the
semantic validator checks this.

\subsection{Expression Language}
\label{subsec:expressions}

v2 retains \textbf{Go templates} (\texttt{text/template}) as the expression
language for all interpolated fields (\texttt{title}, \texttt{args}, \texttt{when},
\texttt{condition}, \texttt{instructions}, etc.).  The decision to retain Go
templates is grounded in two facts:
\begin{enumerate}
  \item The v1 template engine is battle-tested and broadly used in v1 runbooks.
  \item Go templates' pipelines and custom function maps provide sufficient
        expressiveness for operational conditionals.
\end{enumerate}

The template data context is the \emph{run variable map}: all \texttt{vars},
resolved \texttt{inputs}, and accumulated \texttt{captures} merged into a single
flat \texttt{map[string]string}.  Access by dot notation:
\texttt{\{\{\ .hostname\ \}\}}.

\paragraph{Built-in template functions (v2 additions)}

v2 adds a standard function library accessible in all templates:

\begin{center}
\begin{tabular}{ll}
\hline
\textbf{Function} & \textbf{Signature} \\
\hline
\texttt{contains}     & \texttt{(s, substr string) bool} \\
\texttt{hasPrefix}    & \texttt{(s, prefix string) bool} \\
\texttt{hasSuffix}    & \texttt{(s, suffix string) bool} \\
\texttt{trim}         & \texttt{(s string) string} \\
\texttt{toLower}      & \texttt{(s string) string} \\
\texttt{toUpper}      & \texttt{(s string) string} \\
\texttt{split}        & \texttt{(s, sep string) []string} \\
\texttt{join}         & \texttt{(elems []string, sep string) string} \\
\texttt{env}          & \texttt{(name string) string} \\
\texttt{now}          & \texttt{() string} (RFC3339) \\
\texttt{runID}        & \texttt{() string} \\
\texttt{not}          & \texttt{(v bool) bool} \\
\texttt{eq, ne, lt, gt, le, ge} & Standard comparison \\
\texttt{fromYAML}     & \texttt{(s string) interface\{\}} \\
\texttt{fromJSON}     & \texttt{(s string) interface\{\}} \\
\hline
\end{tabular}
\end{center}

All functions from \texttt{text/template} (built-ins: \texttt{and}, \texttt{or},
\texttt{index}, \texttt{len}, \texttt{printf}, etc.) remain available.

\paragraph{\texttt{fromYAML} and \texttt{fromJSON}}
\texttt{fromYAML} and \texttt{fromJSON} parse a raw string variable into a structured
\texttt{interface\{\}} value.  They are the primary mechanism for traversing values
collected from \texttt{text} fields that declare \texttt{format: yaml} or
\texttt{format: json} (see §\ref{sec:collector_field_validation}).  The raw string is
\emph{always} what is stored in the run variable; these functions perform the
parse on each template evaluation.  A parse error in a template expression is a hard
step failure.

\begin{verbatim}
{{/* Access a key from a JSON-typed text field */}}
{{ $cfg := fromJSON .service_config }}
{{ $cfg.region }}

{{/* Access a nested key from a YAML-typed text field */}}
{{ $spec := fromYAML .deploy_spec }}
{{ $spec.replicas }}
\end{verbatim}

\paragraph{Template errors}

In v2, a template evaluation error in a \texttt{condition} or \texttt{when}
field is a hard step failure (not a soft skip).  The step transitions to
\texttt{failed} and execution halts unless \texttt{continue\_on\_fail: true} is
set on the step.

% ============================================================
\section{Step Types (v2 Inventory)}
\label{sec:step-types}
% ============================================================

Each step in \texttt{flow} is a \texttt{TreeNode}: either a \texttt{step} object
(with optional \texttt{branches} and \texttt{iterate} siblings) or a standalone
\texttt{iterate} block.

v2 defines fourteen step types:

\begin{center}
\begin{tabular}{lp{8cm}}
\hline
\textbf{Type} & \textbf{Purpose} \\
\hline
\texttt{cli}              & Execute a local command (\S\ref{subsec:step-cli}) \\
\texttt{choice}           & Operator selects from predefined options (\S\ref{subsec:step-choice}) \\
\texttt{decision}         & Operator selects an execution path (\S\ref{subsec:step-decision}) \\
\texttt{collector}        & Operator provides structured input across 9 field types (\S\ref{subsec:step-collector}) \\
\texttt{approve}          & Standalone approval gate with quorum and business-day timeout (\S\ref{subsec:step-approve}) \\
\texttt{tool}             & Invoke a declared tool action (\S\ref{subsec:step-tool}) \\
\texttt{wait\_for\_event} & Pause until an inbound event arrives (\S\ref{subsec:step-wait-for-event}) \\
\texttt{include}          & Expand another runbook inline into the current run path (\S\ref{subsec:step-include}) \\
\texttt{branch}           & Conditional fan-out to labelled step sequences (\S\ref{subsec:step-branch}) \\
\texttt{iterate}          & Loop over a collection (\S\ref{subsec:step-iterate}) \\
\texttt{parallel}         & Fan-out concurrent branches with join semantics (\S\ref{subsec:step-parallel}) \\
\texttt{assert}           & Evaluate a guard expression and halt on failure (\S\ref{subsec:step-assert}) \\
\texttt{compensate}       & Declare a saga compensation action (\S\ref{subsec:step-compensate}) \\
\texttt{end}              & Declare a terminal outcome (\S\ref{subsec:step-end}) \\
\hline
\end{tabular}
\end{center}

% ── Step-type taxonomy diagram ────────────────────────────────────────────────
\begin{figure}[htbp]
  \centering
  \begin{tikzpicture}[node distance=0.45cm and 0.35cm]

    % ── Execution ─────────────────────────────────────────────────────────────
    \node[catbox] (cat-exec) {Execution};
    \node[stepbox, below=of cat-exec]               (cli)     {cli};
    \node[stepbox, below=0.3cm of cli]              (tool)    {tool};
    \node[stepbox, below=0.3cm of tool]             (include) {include};

    \begin{scope}[on background layer]
      \node[gertfit, fit=(cat-exec)(cli)(tool)(include)] (grp-exec) {};
    \end{scope}

    % ── User Input ────────────────────────────────────────────────────────────
    \node[catbox, right=0.9cm of cat-exec]              (cat-input)  {User Input};
    \node[stepbox, below=of cat-input]                  (choice)     {choice};
    \node[stepbox, below=0.3cm of choice]               (decision)   {decision};
    \node[stepbox, below=0.3cm of decision]             (collector)  {collector};

    \begin{scope}[on background layer]
      \node[gertfit, fit=(cat-input)(choice)(decision)(collector)] (grp-input) {};
    \end{scope}

    % ── Flow Control ──────────────────────────────────────────────────────────
    \node[catbox, right=0.9cm of cat-input]             (cat-flow)   {Flow Control};
    \node[stepbox, below=of cat-flow]                   (branch)     {branch};
    \node[stepbox, below=0.3cm of branch]               (iterate)    {iterate};
    \node[stepbox, below=0.3cm of iterate]              (parallel)   {parallel};

    \begin{scope}[on background layer]
      \node[gertfit, fit=(cat-flow)(branch)(iterate)(parallel)] (grp-flow) {};
    \end{scope}

    % ── Governance ────────────────────────────────────────────────────────────
    \node[catbox, right=0.9cm of cat-flow]              (cat-gov)    {Governance};
    \node[stepbox, below=of cat-gov]                    (assert)     {assert};
    \node[stepbox, below=0.3cm of assert]               (compensate) {compensate};
    \node[stepbox, below=0.3cm of compensate]           (approve)    {approve};
    \node[stepbox, below=0.3cm of approve]              (wfe)        {wait\_for\_event};

    \begin{scope}[on background layer]
      \node[gertfit, fit=(cat-gov)(assert)(compensate)(approve)(wfe)] (grp-gov) {};
    \end{scope}

    % ── Terminal ──────────────────────────────────────────────────────────────
    \node[catbox, right=0.9cm of cat-gov]               (cat-term)   {Terminal};
    \node[stepbox, below=of cat-term]                   (end)        {end};

    \begin{scope}[on background layer]
      \node[gertfit, fit=(cat-term)(end)] (grp-term) {};
    \end{scope}

  \end{tikzpicture}
  \caption{Step type taxonomy — 14 types organized by category.}
  \label{fig:step-type-taxonomy}
\end{figure}

\subsection{Common Step Fields}
\label{subsec:common-fields}

The following fields are available on every step type:

\begin{center}
\begin{tabular}{lllp{5.5cm}}
\hline
\textbf{Field} & \textbf{Type} & \textbf{Required} & \textbf{Description} \\
\hline
\texttt{id}              & string  & Yes & Unique within the runbook \\
\texttt{type}            & enum    & Yes & Step type (see below) \\
\texttt{title}           & string  & No  & Template; displayed in TUI/trace \\
\texttt{subtitle}        & string  & No  & Secondary template annotation \\
\texttt{when}            & string  & No  & Guard expression; skips step if false \\
\texttt{timeout}         & string  & No  & Duration (e.g.\ \texttt{30s}); overrides default \\
\texttt{delay}           & string  & No  & Pre-execution pause (e.g.\ \texttt{5s}) \\
\texttt{retry}           & object  & No  & Retry config (\S\ref{subsec:retry}) \\
\texttt{continue\_on\_fail} & bool & No  & Continue execution on failure \\
\texttt{scope}           & string  & No  & Named scope for variable isolation \\
\texttt{export}          & string array & No & Variables to export from scope \\
\texttt{capture}         & map     & No  & Output capture bindings \\
\texttt{contract}        & object  & No  & Behavioral contract for governance \\
\hline
\end{tabular}
\end{center}

\paragraph{Retry configuration}
\label{subsec:retry}

\begin{minted}{yaml}
retry:
  max: 3              # maximum retry attempts (required)
  interval: "5s"      # initial wait between attempts
  backoff: linear     # linear | exponential (default: linear)
\end{minted}

\paragraph{Step contract}

The \texttt{contract} field annotates a step with its behavioral properties for
governance policy evaluation:

\begin{minted}{yaml}
contract:
  effects:
    - network.probe
    - service.restart
  reads:   [hostname, port]
  writes:  [http_result]
  deterministic: true
  idempotent: false
  secrets: [auth_token]
\end{minted}

Effects are free-form strings matched against \texttt{governance.rules[].effects}.
The \texttt{secrets} list names variables whose values must be redacted in traces.

\subsection{Step Type: cli}
\label{subsec:step-cli}

Executes a local command directly.  In the v2 runtime \texttt{cli} is the
canonical command step; the v1 \texttt{with.argv} form is accepted in
compatibility mode and rewritten to \texttt{cli} by the migration tool.

\texttt{type: cli} supports two mutually exclusive input modes:

\begin{itemize}
  \item \textbf{Exec form} (\texttt{command} + \texttt{args}): the executor
        invokes the binary directly without a shell.  No shell expansion occurs;
        arguments are passed verbatim (after template substitution).
  \item \textbf{Run form} (\texttt{run}): a shell-script string or a map of
        per-shell scripts is passed to a shell process.  The \texttt{shell}
        field controls which shell is used when \texttt{run} is a string.
\end{itemize}

Using both \texttt{command}/\texttt{args} and \texttt{run} in the same step is
a \textbf{schema validation error}.

\begin{center}
\begin{tabular}{lllp{5.2cm}}
\hline
\textbf{Field}   & \textbf{Type}            & \textbf{Required} & \textbf{Description} \\
\hline
\texttt{command} & string                   & See note & Executable name or full path.
                                                         Mutually exclusive with \texttt{run}. \\
\texttt{args}    & string array             & No       & Arguments (templates expanded).
                                                         Only valid with \texttt{command}. \\
\texttt{run}     & string \textbar{} map    & See note & Shell-string or per-shell map.
                                                         Mutually exclusive with \texttt{command}. \\
\texttt{shell}   & string                   & No       & Shell to use with \texttt{run} string form.
                                                         Enum: \texttt{auto | bash | sh | pwsh | cmd}.
                                                         Default: \texttt{auto}.
                                                         Ignored when \texttt{run} is a map
                                                         (validation warning if both are present). \\
\texttt{env}     & map                      & No       & Additional environment variables \\
\texttt{workdir} & string                   & No       & Working directory \\
\texttt{stdin}   & string                   & No       & Template string piped to stdin \\
\texttt{capture} & map                      & No       & \texttt{stdout | stderr | exit\_code} → var \\
\hline
\end{tabular}
\end{center}

\textit{Note:} Exactly one of \texttt{command} or \texttt{run} must be present.

Governance checks against the \texttt{allowed\_commands} / \texttt{denied\_commands}
lists and any matching \texttt{governance.rules[].effects} are evaluated
before the command executes.  If the rule action is \texttt{require-approval},
the step pauses for human approval before proceeding.

\paragraph{Run form: string}

When \texttt{run} is a string, the executor spawns the shell specified by the
\texttt{shell} field and passes the string as a single script argument (equivalent
to \texttt{shell -c "script"}).  When \texttt{shell: auto} (the default), the
executor selects the platform-native shell: \texttt{bash} on Linux/macOS,
\texttt{pwsh} on Windows (falling back to \texttt{cmd} if PowerShell is absent).

\paragraph{Run form: map}

When \texttt{run} is a map, the keys are shell names (\texttt{bash}, \texttt{sh},
\texttt{pwsh}, \texttt{cmd}) and the values are shell-script strings.  The
executor selects the entry that matches the shell available on the current host.

\subparagraph{Map key resolution order}

\begin{enumerate}
  \item \textbf{Exact match}: the host's configured or detected shell matches a
        map key (e.g.\ host has \texttt{bash} → use \texttt{bash} entry).
  \item \textbf{Platform-family fallback}: if the exact match fails, fall back
        according to the table below.
  \item \textbf{No match}: the step \textbf{fails} with the error:
        \textit{``No matching shell entry for current platform.
        Available entries: \{keys\}.  Platform shell: \{detected\}.''}
\end{enumerate}

\begin{center}
\begin{tabular}{ll}
\hline
\textbf{Requested shell} & \textbf{Falls back to} \\
\hline
\texttt{bash} & \texttt{sh} \\
\texttt{sh}   & (none — fail) \\
\texttt{pwsh} & \texttt{cmd} (Windows only) \\
\texttt{cmd}  & (none — fail) \\
\hline
\end{tabular}
\end{center}

When \texttt{run} is a map and a \texttt{shell} field is also present, the
\texttt{shell} field is \textbf{ignored} and the runtime emits a validation
warning: \textit{``shell field is ignored when run is a map.  Remove shell or
convert run to a string.''}.

\paragraph{Example: exec form (unchanged)}

\begin{verbatim}
- step:
    id: tail_logs
    type: cli
    title: "Tail last 100 lines from {{ .log_file }}"
    command: tail
    args: ["-n", "100", "{{ .log_file }}"]
    capture:
      output: stdout
      exit: exit_code
    contract:
      effects: [filesystem.read]
      reads: [log_file]
      writes: [output]
\end{verbatim}

\paragraph{Example: run string form with explicit shell}

\begin{verbatim}
- step:
    id: run_script
    type: cli
    shell: bash
    run: |
      if [ "{{ .env }}" = "prod" ]; then
        echo "Production deploy"
      fi
    capture:
      stdout: script_output
\end{verbatim}

\paragraph{Example: run string form with auto shell}

\begin{verbatim}
- step:
    id: echo_env
    type: cli
    run: echo "Environment is {{ .env }}"
    capture:
      stdout: echo_result
\end{verbatim}

\paragraph{Example: run map form (multi-shell)}

\begin{verbatim}
- step:
    id: clear_cache
    type: cli
    run:
      bash: rm -rf /tmp/cache/{{ .service }}
      cmd: rd /s /q C:\tmp\cache\{{ .service }}
      pwsh: Remove-Item -Recurse -Force C:\tmp\cache\{{ .service }}
    capture:
      exit_code: clear_result
\end{verbatim}

% ─────────────────────────────────────────────────────────────────────────────
% Figure: CLI step execution flow
% ─────────────────────────────────────────────────────────────────────────────
\begin{figure}[H]
\centering
\resizebox{\linewidth}{!}{%
\begin{tikzpicture}[node distance=0.5cm and 0.9cm, scale=0.85, transform shape]
  \node[stepbox] (cli-parse)   {Parse Step\\Fields};
  \node[stepbox, right=of cli-parse]   (cli-gov)     {Governance\\Check};
  \node[stepbox, right=of cli-gov]     (cli-shell)   {Resolve Shell\\(\texttt{auto/bash/sh})};
  \node[stepbox, right=of cli-shell]   (cli-build)   {Build Command\\or Script};
  \node[stepbox, right=of cli-build]   (cli-exec)    {Exec Process\\(fork/exec)};
  \node[stepbox, right=of cli-exec]    (cli-capture) {Capture\\stdout/stderr};
  \node[stepbox, right=of cli-capture] (cli-exit)    {Evaluate\\Exit Code};

  %% Terminal outcomes
  \node[stepbox, below=1.2cm of cli-gov,  fill=black!10] (cli-blocked) {BLOCKED\\(denied)};
  \node[stepbox, below=1.2cm of cli-exit, fill=black!3]  (cli-pass)    {PASSED};
  \node[stepbox, below right=0.5cm and 0.1cm of cli-exit, fill=black!10] (cli-fail) {FAILED\\(non-zero exit)};

  %% Main flow arrows
  \draw[gertarrow] (cli-parse)   -- (cli-gov);
  \draw[gertarrow] (cli-gov)     -- (cli-shell);
  \draw[gertarrow] (cli-shell)   -- (cli-build);
  \draw[gertarrow] (cli-build)   -- (cli-exec);
  \draw[gertarrow] (cli-exec)    -- (cli-capture);
  \draw[gertarrow] (cli-capture) -- (cli-exit);

  %% Branch arrows
  \draw[gertarrow] (cli-gov)  -- node[left, font=\scriptsize\itshape] {denied} (cli-blocked);
  \draw[gertarrow] (cli-exit) -- node[left, font=\scriptsize\itshape] {exit=0} (cli-pass);
  \draw[gertarrow] (cli-exit) -| node[right, font=\scriptsize\itshape, pos=0.25] {exit$\neq$0} (cli-fail);
\end{tikzpicture}}
\caption{\texttt{type: cli} step execution flow: from field parsing through governance check, shell resolution, command building, process execution, output capture, and exit-code evaluation.}
\label{fig:cli-step-execution}
\end{figure}

\subsection{Step Type: choice}
\label{subsec:step-choice}

Prompts the user to select from a set of predefined options and stores the
result in a variable.  This is a value-capture primitive, analogous to a form
field or radio-button group.  The selected value is available to subsequent
steps via variable substitution.

\begin{center}
\begin{tabular}{lllp{5cm}}
\hline
\textbf{Field} & \textbf{Type} & \textbf{Required} & \textbf{Description} \\
\hline
\texttt{prompt}         & string  & Yes & Question or prompt displayed to user \\
\texttt{options}        & array   & Yes & List of option objects \\
\texttt{variable}       & string  & Yes & Variable name to store selected value \\
\texttt{default}        & string  & No  & Default option value if user skips \\
\texttt{multiple}       & bool    & No  & Allow selecting more than one option (default: false) \\
\texttt{min\_selections} & integer & No  & Minimum number of selections when \texttt{multiple: true} \\
\texttt{max\_selections} & integer & No  & Maximum number of selections when \texttt{multiple: true} \\
\hline
\end{tabular}
\end{center}

When \texttt{multiple: true} the stored variable becomes an array of selected values
rather than a single string.  The first selected value is used when a downstream
template expression expects a scalar.

\paragraph{Option structure}

Each option is an object with:

\begin{center}
\begin{tabular}{lllp{5cm}}
\hline
\textbf{Field} & \textbf{Type} & \textbf{Required} & \textbf{Description} \\
\hline
\texttt{label} & string & Yes & Display text shown to user \\
\texttt{value} & string & Yes & Value stored in variable when selected \\
\texttt{hint}  & string & No  & Additional context or help text \\
\hline
\end{tabular}
\end{center}

\paragraph{Constraints}

\begin{itemize}
  \item At least 2 options must be defined.
  \item All \texttt{option.value} entries must be unique within a step.
  \item If \texttt{default} is provided, it must match one of the option values.
  \item The \texttt{variable} name follows standard identifier rules (alphanumeric + underscore).
  \item When \texttt{multiple: true}, \texttt{min\_selections} and \texttt{max\_selections}
        are optional but recommended to constrain the selection count.
  \item \texttt{min\_selections} must be $\geq 1$; \texttt{max\_selections} must be
        $\leq$ the total number of options and $\geq$ \texttt{min\_selections}.
\end{itemize}

\paragraph{Example: Environment selection}

\begin{minted}{yaml}
- step:
    id: select_environment
    type: choice
    title: Select deployment target
    prompt: Which environment are you deploying to?
    options:
      - label: Staging
        value: staging
        hint: Non-production environment for validation
      - label: Production
        value: production
        hint: Live customer-facing environment
      - label: Canary (10%)
        value: canary
        hint: Gradual rollout to 10% of production traffic
    variable: deploy_env
    default: staging
\end{minted}

The value \texttt{staging}, \texttt{production}, or \texttt{canary} is stored in
\texttt{\{\{ .deploy\_env \}\}} and can be referenced by subsequent steps.

\subsection{Step Type: decision}
\label{subsec:step-decision}

Presents the user with 2 or more execution paths and routes the flow to the
selected one.  This is a control-flow primitive: the user picks a branch, and
execution forks to a different runbook or labeled section.  Unlike
\texttt{choice}, which merely stores a value, \texttt{decision} directs the
execution path itself.

Also acceptable name: \texttt{router} (alias for \texttt{decision}).

\begin{center}
\begin{tabular}{lllp{5cm}}
\hline
\textbf{Field} & \textbf{Type} & \textbf{Required} & \textbf{Description} \\
\hline
\texttt{prompt}   & string  & Yes & Question or scenario description \\
\texttt{routes}   & array   & Yes & List of route objects (minimum 2) \\
\texttt{variable} & string  & No  & Variable to store selected route label (audit) \\
\hline
\end{tabular}
\end{center}

\paragraph{Route structure}

Each route is an object with:

\begin{center}
\begin{tabular}{lllp{5cm}}
\hline
\textbf{Field} & \textbf{Type} & \textbf{Required} & \textbf{Description} \\
\hline
\texttt{label}   & string & Yes & Display text shown to user \\
\texttt{runbook} & string & No  & Runbook ID to invoke (mutually exclusive with \texttt{goto}) \\
\texttt{goto}    & string & No  & Step ID to jump to within current runbook \\
\texttt{hint}    & string & No  & Additional context or help text \\
\hline
\end{tabular}
\end{center}

\paragraph{Constraints}

\begin{itemize}
  \item At least 2 routes must be defined.
  \item Each route must specify exactly one of \texttt{runbook} or \texttt{goto}, not both.
  \item All \texttt{route.label} entries must be unique within a step.
  \item If \texttt{variable} is provided, it stores the selected route's \texttt{label}
        for audit trails.
\end{itemize}

\paragraph{Example: Incident response fork}

\begin{minted}{yaml}
- step:
    id: triage_decision
    type: decision
    title: Choose incident response path
    prompt: |
      Based on the error rate and customer impact, which response
      path should we take?
    routes:
      - label: Standard mitigation
        runbook: incident-standard-mitigation
        hint: Error rate < 5%, no customer escalations
      - label: Emergency rollback
        runbook: incident-emergency-rollback
        hint: Error rate > 10% or critical customer impact
      - label: Escalate to on-call
        goto: escalate_to_oncall
        hint: Unclear root cause; needs senior diagnosis
    variable: chosen_path
\end{minted}

After the user selects a route, execution continues at the target runbook or
step.  The \texttt{chosen\_path} variable contains the selected label
(\texttt{"Standard mitigation"}, \texttt{"Emergency rollback"}, or
\texttt{"Escalate to on-call"}) for audit purposes.

\subsection{Step Type: collector}
\label{subsec:step-collector}

Prompts the user to provide structured input across a rich set of field types:
free-form text, numbers, dates, booleans, dropdowns, file attachments, and more.
All fields in a single \texttt{collector} step are presented and submitted
atomically — the operator fills them all in and confirms once.
The collected values are stored as named variables or artifacts for downstream
steps and evidence capture.  This replaces the overly-generic \texttt{manual}
step type from early v2 drafts.

\begin{center}
\begin{tabular}{lllp{5cm}}
\hline
\textbf{Field} & \textbf{Type} & \textbf{Required} & \textbf{Description} \\
\hline
\texttt{prompt}     & string  & Yes & Markdown instructions displayed to user \\
\texttt{fields}     & array   & Yes & List of field objects to collect (minimum 1) \\
\texttt{approvals}  & object  & No  & Approval gate config (\S\ref{subsec:approval-gate}) \\
\hline
\end{tabular}
\end{center}

\paragraph{Field structure}

Each field is an object with:

\begin{center}
\begin{tabular}{lllp{4cm}}
\hline
\textbf{Field} & \textbf{Type} & \textbf{Required} & \textbf{Description} \\
\hline
\texttt{name}       & string  & Yes & Variable name to store collected value \\
\texttt{type}       & enum    & Yes & Field type — see field type inventory below \\
\texttt{label}      & string  & Yes & Prompt or label text shown to user \\
\texttt{required}   & bool    & No  & If true, user must provide a value (default: false) \\
\texttt{when}       & template & No  & Go template — field shown and prompted only when truthy; variable binding skipped when falsy \\
\texttt{hint}       & string  & No  & Additional context or validation help \\
\texttt{default}    & any     & No  & Pre-filled default value (must match field type) \\
\texttt{validation} & object  & No  & Type-specific validation constraints \\
\texttt{options}    & array   & No  & Option list for \texttt{select} type \\
\texttt{options\_from} & object & No & Dynamic option list provider for \texttt{select} and \texttt{autocomplete} types \\
\texttt{multiple}   & bool    & No  & Allow multiple selections for \texttt{select} and \texttt{autocomplete} types (default: false) \\
\texttt{multiline}  & bool    & No  & Enable multi-line textarea for \texttt{text} type (default: false) \\
\texttt{ephemeral}  & bool    & No  & Value is never written to the audit log and never persisted; in-memory only for the duration of the run (default: false) \\
\hline
\end{tabular}
\end{center}

\paragraph{Ephemeral fields}
\label{par:ephemeral-fields}

When a field carries \texttt{ephemeral: true}, gert applies the following
constraints to the collected value:

\begin{itemize}
  \item \textbf{Variable binding:} The value \emph{is} bound into the runbook
        variable space and may be used in downstream template expressions (e.g.\
        \texttt{\{\{ .deploy\_token \}\}}) for the duration of the current run.
  \item \textbf{Audit log:} The audit record for this field writes
        \texttt{"[REDACTED]"} instead of the actual value.  The field name,
        type, and timestamp are still recorded; only the value is suppressed.
  \item \textbf{Persistence:} The value is \emph{never} written to any store —
        not the JSONL trace, not the evidence artifact store, not any variable
        snapshot.  It exists only in the in-memory execution context.
  \item \textbf{Run suspension:} If the run is suspended after collection (e.g.\
        after a \texttt{wait\_for\_event} step) and then resumed, ephemeral
        field values are gone.  The \texttt{collector} step must be re-executed
        and the operator must re-supply the values.
\end{itemize}

\texttt{ephemeral: true} is valid on any field type, but is semantically
meaningful on \texttt{text}, \texttt{select}, and \texttt{boolean} fields.
Setting it on \texttt{file} or \texttt{image} fields is a validation
\emph{warning} (not an error) — file content is large and a
\texttt{[REDACTED]} placeholder in the audit trail is still useful; however,
the \emph{file payload} will not be persisted to the artifact store.

\begin{quote}
\textbf{Security note:} Ephemeral fields reduce audit footprint for secrets.
They do \emph{not} prevent the value from being transmitted to tool steps or
sub-processes.  Authors are responsible for ensuring ephemeral values are only
passed to secure execution contexts.
\end{quote}

\begin{minted}{yaml}
- id: collect_credentials
  type: collector
  label: "Deploy credentials"
  fields:
    - id: deploy_user
      type: text
      label: "Service account username"
      required: true
    - id: deploy_token
      type: text
      label: "Deploy token"
      required: true
      ephemeral: true    # never written to audit log or persisted
    - id: target_env
      type: select
      label: "Target environment"
      required: true
      options:
        - { value: "staging",    label: "Staging" }
        - { value: "production", label: "Production" }
\end{minted}

\paragraph{Dynamic Forms}
\label{par:dynamic-forms}

When one or more fields carry a \texttt{when} expression, the \texttt{collector}
step renders as a \emph{dynamic form}: fields are shown, hidden, and re-evaluated
as the operator fills in earlier fields.  gert enforces the following evaluation
order guarantees:

\begin{enumerate}
  \item Fields are evaluated in the order they appear in the \texttt{fields}
        array.
  \item A field's \texttt{when} expression may reference only variables that
        are declared \emph{earlier} in the same \texttt{fields} array, or
        variables already present in the global runbook variable space
        (variables set by prior steps, runbook inputs, or \texttt{gert.*}
        built-ins).  A forward reference — referencing a field declared
        \emph{later} in the same \texttt{fields} array — is a schema
        validation error detected at parse time.
  \item When a field's \texttt{when} expression evaluates to falsy: the field
        is hidden in the UI, not prompted in a TUI, and its variable binding is
        \emph{skipped}.  The variable does not appear in the runbook variable
        space; it is not set to \texttt{null} or empty string.
  \item In interactive UIs (VS~Code, browser), if an earlier field value
        changes after later fields have been filled, all downstream
        \texttt{when} expressions are re-evaluated and dependent fields are
        shown or hidden accordingly.
  \item A \texttt{required: true} field with a \texttt{when} expression is
        required only when its \texttt{when} condition is truthy.  When the
        condition is falsy, the \texttt{required} constraint does not apply.
\end{enumerate}

\paragraph{Field type inventory}
\label{par:collector-field-types}

\texttt{collector} supports ten field types:

\begin{center}
\begin{tabular}{lp{4.5cm}p{4.5cm}}
\hline
\textbf{Type} & \textbf{Description} & \textbf{Notes} \\
\hline
\texttt{text}         & Single-line or multi-line text input & Set \texttt{multiline: true} for textarea; \texttt{format} for structured validation \\
\texttt{number}       & Integer or float numeric input & Supports \texttt{validation.min}, \texttt{max}, \texttt{step} \\
\texttt{integer}      & Integer-only numeric input & Shorthand for \texttt{number} with \texttt{step: 1} \\
\texttt{date}         & ISO 8601 date (YYYY-MM-DD) & UI hint: date picker \\
\texttt{datetime}     & RFC 3339 datetime with timezone & UI hint: datetime picker \\
\texttt{boolean}      & True/false checkbox & Stored as boolean in variable space \\
\texttt{select}       & Dropdown or option list & Set \texttt{multiple: true} for multi-select \\
\texttt{autocomplete} & Live search text field & Options fetched from provider as user types; degrades to \texttt{select} in CLI \\
\texttt{file}         & File attachment & Stored as artifact with SHA256 hash \\
\texttt{image}        & Image/screenshot attachment & Stored as artifact \\
\hline
\end{tabular}
\end{center}

\subsubsection{Field type: \texttt{text}}
\label{subsubsec:field-text}

Single-line text input by default.  Set \texttt{multiline: true} to render a
multi-line textarea — appropriate for descriptions, reproduction steps, or
any field where newlines are meaningful.

\paragraph{Text field validation constraints}

The optional \texttt{validation} object on a \texttt{text} field supports five
sub-fields:

\begin{center}
\begin{tabular}{lllp{5cm}}
\hline
\textbf{Validation field} & \textbf{Type} & \textbf{Required} & \textbf{Description} \\
\hline
\texttt{pattern}       & string  & No & ECMA~262 regular expression; field value must match \\
\texttt{pattern\_hint} & string  & No & Human-readable error shown when pattern validation fails \\
\texttt{min\_length}   & integer & No & Minimum character count ($\geq 0$) \\
\texttt{max\_length}   & integer & No & Maximum character count ($> 0$; must exceed \texttt{min\_length} when both are present) \\
\texttt{format}        & enum    & No & Structural parse validation for \texttt{multiline: true} fields: \texttt{yaml}, \texttt{json}, or \texttt{toml} \\
\hline
\end{tabular}
\end{center}

\begin{minted}{yaml}
# Basic multiline with hint
- name: description
  type: text
  label: Incident description
  multiline: true
  required: true
  hint: Describe what happened, when, and which services were affected

# Pattern validation — IPv4 address
- name: target_ip
  type: text
  label: Target IP address
  required: true
  validation:
    pattern: "^((25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\\.){3}(25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$"
    pattern_hint: "Must be a valid IPv4 address (e.g. 192.168.1.1)"

# Length constraints
- name: summary
  type: text
  label: Executive summary
  required: true
  validation:
    min_length: 20
    max_length: 500
\end{minted}

\paragraph{Structured multiline format validation}
\label{par:text-format}
\label{sec:collector_field_validation}

When a \texttt{text} field has \texttt{multiline: true}, the optional
\texttt{validation.format} field declares that the submitted value must be
parseable as a specific structured data format:

\begin{itemize}
  \item \texttt{format: yaml} — value must parse as valid YAML.
  \item \texttt{format: json} — value must parse as valid JSON.
  \item \texttt{format: toml} — value must parse as valid TOML~v2.1.
        \emph{Note: TOML format validation is deferred to a future release;
        declaring it is a validation warning in v2.0.}
  \item No \texttt{format} (default) — plain text; no structural validation.
\end{itemize}

Parse validation occurs \emph{after submission}.  On failure, the operator
is re-prompted with the message:
\texttt{"Value must be valid \{FORMAT\}.  Please correct and resubmit."}
On success, the value is stored as a raw string — the original text, not a
parsed object.  Parsing is validation only.

\begin{quote}
\textbf{Note:} The stored value is always a string.  Downstream template
expressions that need to traverse the structure must use gert's
\texttt{fromYAML}/\texttt{fromJSON} template functions.
\end{quote}

\begin{minted}{yaml}
# YAML-format multiline field
- id: config_patch
  type: text
  label: "Configuration patch (YAML)"
  multiline: true
  required: true
  validation:
    format: yaml
    min_length: 10

# JSON-format multiline field
- id: event_payload
  type: text
  label: "Event payload (JSON)"
  multiline: true
  required: true
  validation:
    format: json
\end{minted}

\texttt{validation.format} is silently ignored when \texttt{multiline} is
\texttt{false} or absent — a validation warning is emitted at schema load
time.

\subsubsection{Field type: \texttt{number}}
\label{subsubsec:field-number}

Integer or floating-point numeric input.  The optional \texttt{validation}
object constrains acceptable values:

\begin{center}
\begin{tabular}{lllp{5cm}}
\hline
\textbf{Validation field} & \textbf{Type} & \textbf{Required} & \textbf{Description} \\
\hline
\texttt{min}  & number  & No & Minimum acceptable value (inclusive) \\
\texttt{max}  & number  & No & Maximum acceptable value (inclusive) \\
\texttt{step} & number  & No & Granularity; set to \texttt{1} to enforce integers \\
\hline
\end{tabular}
\end{center}

\begin{minted}{yaml}
- name: amount_usd
  type: number
  label: Amount (USD)
  required: true
  validation:
    min: 0
    max: 10000000
- name: port_number
  type: number
  label: Port number
  required: true
  validation:
    min: 1
    max: 65535
    step: 1
- name: severity_score
  type: number
  label: Severity score (1.0 – 10.0)
  required: true
  validation:
    min: 1.0
    max: 10.0
    step: 0.1
\end{minted}

\subsubsection{Field type: \texttt{integer}}
\label{subsubsec:field-integer}

Convenience shorthand for \texttt{number} with \texttt{step: 1}.  The stored
variable value is always an integer.  Accepts the same \texttt{validation.min}
and \texttt{validation.max} fields as \texttt{number}.

\begin{minted}{yaml}
- name: instance_count
  type: integer
  label: Number of instances
  required: true
  validation:
    min: 1
    max: 100
\end{minted}

\subsubsection{Field type: \texttt{date}}
\label{subsubsec:field-date}

ISO 8601 date input (\texttt{YYYY-MM-DD}).  The UI renders a date picker.
The stored variable value is the ISO date string.

\begin{center}
\begin{tabular}{lllp{5cm}}
\hline
\textbf{Validation field} & \textbf{Type} & \textbf{Required} & \textbf{Description} \\
\hline
\texttt{min} & ISO date string & No & Earliest acceptable date (inclusive) \\
\texttt{max} & ISO date string & No & Latest acceptable date (inclusive) \\
\hline
\end{tabular}
\end{center}

\begin{minted}{yaml}
- name: start_date
  type: date
  label: Start date
  required: true
  validation:
    min: "2024-01-01"
\end{minted}

\subsubsection{Field type: \texttt{datetime}}
\label{subsubsec:field-datetime}

RFC 3339 datetime input with explicit timezone offset or \texttt{Z} for UTC
(e.g.\ \texttt{2026-04-19T14:30:00-05:00}).  The UI renders a datetime picker.

\begin{center}
\begin{tabular}{lllp{5cm}}
\hline
\textbf{Validation field} & \textbf{Type} & \textbf{Required} & \textbf{Description} \\
\hline
\texttt{min} & RFC 3339 string & No & Earliest acceptable datetime (inclusive) \\
\texttt{max} & RFC 3339 string & No & Latest acceptable datetime (inclusive) \\
\hline
\end{tabular}
\end{center}

\begin{minted}{yaml}
- name: incident_detected_at
  type: datetime
  label: When was the incident first detected?
  required: true
\end{minted}

\subsubsection{Field type: \texttt{boolean}}
\label{subsubsec:field-boolean}

True/false checkbox.  The stored variable value is a boolean (\texttt{true}
or \texttt{false}), not a string.  No additional validation fields are defined.

\begin{minted}{yaml}
- name: background_check_completed
  type: boolean
  label: Background check completed?
  required: true
- name: no_disqualifying_issues
  type: boolean
  label: No disqualifying issues found?
  required: true
\end{minted}

The value is available in templates as a boolean:
\texttt{\{\{ if .background\_check\_completed \}\}…\{\{ end \}\}}.

\subsubsection{Field type: \texttt{select}}
\label{subsubsec:field-select}

Dropdown or option list.  Options are defined inline via \texttt{options}
(static list) or loaded at runtime via \texttt{options\_from} (dynamic list
from a provider).  Exactly one of \texttt{options} or \texttt{options\_from}
must be present.

\paragraph{options — static list}

\begin{center}
\begin{tabular}{lllp{5cm}}
\hline
\textbf{Field} & \textbf{Type} & \textbf{Required} & \textbf{Description} \\
\hline
\texttt{value} & string & Yes & Value stored in variable when selected \\
\texttt{label} & string & Yes & Display text shown to user \\
\hline
\end{tabular}
\end{center}

\paragraph{options\_from — dynamic provider}

\begin{center}
\begin{tabular}{lllp{5cm}}
\hline
\textbf{Field} & \textbf{Type} & \textbf{Required} & \textbf{Description} \\
\hline
\texttt{provider} & string & Yes & Name of the input provider to query \\
\texttt{field}    & string & No  & Provider response field to use as option list \\
\hline
\end{tabular}
\end{center}

\texttt{options} and \texttt{options\_from} are mutually exclusive.  When
\texttt{multiple: true} is set, the operator may select more than one option
and the stored variable becomes an array of selected values.

\begin{minted}{yaml}
# Static options
- name: department
  type: select
  label: Department
  required: true
  options:
    - value: engineering
      label: Engineering
    - value: sales
      label: Sales
    - value: marketing
      label: Marketing
    - value: finance
      label: Finance

# Dynamic options from provider
- name: budget_line_item
  type: select
  label: Budget line item
  required: true
  options_from:
    provider: finance-system
    field: budget_line_items

# Multi-select
- name: affected_services
  type: select
  label: Affected services
  required: true
  multiple: true
  options:
    - value: api-gateway
      label: API Gateway
    - value: auth-service
      label: Auth Service
    - value: payments
      label: Payments
\end{minted}

\subsubsection{Field type: \texttt{autocomplete}}
\label{subsubsec:field-autocomplete}

A text field with live search: as the operator types, the configured
\texttt{options\_from} provider is queried and matching options are returned
in real time.  Useful for searching service names, users, ticket IDs,
configuration keys, and any other dataset too large to enumerate statically.

\texttt{autocomplete} behaves like \texttt{select} in its storage and
validation semantics — the selected value is stored as a string (or an
array of strings when \texttt{multiple: true}).  It differs in that options
are fetched dynamically on each keystroke rather than declared inline.

The \texttt{options\_from} object for an \texttt{autocomplete} field extends
the \texttt{select} variant with two additional search-control fields:

\begin{center}
\begin{tabular}{lllp{4.5cm}}
\hline
\textbf{options\_from field} & \textbf{Type} & \textbf{Required} & \textbf{Description} \\
\hline
\texttt{provider}     & string  & Yes & Input provider ID that handles search queries \\
\texttt{field}        & string  & Yes & Provider input field that receives the search query string \\
\texttt{min\_chars}   & integer & No  & Minimum characters typed before search fires (default: 2) \\
\texttt{debounce\_ms} & integer & No  & Debounce delay in milliseconds (default: 300) \\
\hline
\end{tabular}
\end{center}

\begin{quote}
\textbf{CLI / non-interactive fallback:} Providers that do not support
live search \emph{must} gracefully degrade to a \texttt{select} by calling
the provider once with an empty query string to fetch all available options.
The resulting list is then rendered as a standard dropdown.
\end{quote}

\begin{minted}{yaml}
# Live service search
- name: affected_service
  type: autocomplete
  label: "Affected service"
  required: true
  options_from:
    provider: service-registry
    field: query
    min_chars: 2
    debounce_ms: 300

# Multi-select user lookup
- name: reviewers
  type: autocomplete
  label: "Reviewers"
  multiple: true
  options_from:
    provider: hr-directory
    field: query
    min_chars: 2
\end{minted}

\paragraph{Approval gate}
\label{subsec:approval-gate}

If the \texttt{approvals} field is present, the step requires explicit approval
from designated roles before proceeding.

\begin{center}
\begin{tabular}{lllp{4cm}}
\hline
\textbf{Field} & \textbf{Type} & \textbf{Required} & \textbf{Description} \\
\hline
\texttt{min}          & integer & Yes & Minimum approvals required \\
\texttt{roles}        & array   & Yes & List of role names authorized to approve \\
\texttt{timeout}      & string  & No  & Duration before timeout (e.g.\ \texttt{4h}) \\
\texttt{on\_timeout}  & enum    & No  & \texttt{escalate}, \texttt{fail}, \texttt{skip} \\
\texttt{escalate\_to} & array   & No  & Roles to escalate to if timeout occurs \\
\hline
\end{tabular}
\end{center}

The \texttt{timeout} and \texttt{on\_timeout} fields are new in v2, addressing
the SLA enforcement gap identified in the research brief.

\paragraph{Example: Incident evidence collection}

\begin{minted}{yaml}
- step:
    id: collect_evidence
    type: collector
    title: Collect incident evidence
    prompt: |
      Gather all relevant evidence for the incident investigation.
      Include screenshots, error logs, and a summary of impact.
    fields:
      - name: incident_summary
        type: text
        label: Incident summary
        multiline: true
        required: true
        hint: Describe what happened, when, and what services were affected
      - name: error_log
        type: file
        label: Error log file
        required: true
        hint: Attach the relevant error log from the affected service
      - name: dashboard_screenshot
        type: image
        label: Monitoring dashboard screenshot
        required: false
        hint: Screenshot showing metrics during the incident
      - name: related_ticket
        type: url
        label: Related ticket or alert URL
        required: false
    approvals:
      min: 1
      roles: [DRI, incident-commander]
      timeout: 30m
      on_timeout: fail
\end{minted}

After the user provides the requested evidence and approval is granted, the
collected values are available as \texttt{\{\{ .incident\_summary \}\}},
\texttt{\{\{ .error\_log \}\}}, etc.  File and image attachments are stored as
artifacts in the execution trace with SHA256 hashes for tamper-evidence.

\paragraph{Questionnaire Pattern}
\label{par:questionnaire-pattern}

A \texttt{collector} step with multiple \texttt{fields} is the canonical way
to present a questionnaire in gert v2.  All fields are presented and submitted
atomically: the operator fills in the entire form and confirms once, rather than
answering questions in sequential individual steps.  This pattern is preferred
over a series of single-field \texttt{collector} steps when the questions are
logically related and should be answered together.

Benefits of the questionnaire pattern:
\begin{itemize}
  \item Single approval gate covers all related data.
  \item Atomic submission — partial forms cannot leave variables in inconsistent state.
  \item Cleaner audit trail: one evidence entry per logical form.
  \item Better UX: operator sees the full context before filling in any field.
\end{itemize}

\textbf{Example: Incident triage form} (4-field form with conditional logic using \texttt{when})

\begin{verbatim}
- step:
    id: collect_incident_details
    type: collector
    title: Incident Triage
    prompt: |
      Complete the incident triage form.
    fields:
      - name: severity
        type: select
        label: Severity
        required: true
        options:
          - value: low
            label: Low
          - value: medium
            label: Medium
          - value: critical
            label: Critical
      # Shown only when severity = critical (forward-reference guard)
      - name: escalation_reason
        type: text
        label: Escalation reason
        multiline: true
        required: true
        when: "{{ eq .severity 'critical' }}"
      - name: affected_systems
        type: text
        label: Affected systems
        required: true
      # Shown for medium and critical; hidden for low
      - name: customer_impact
        type: boolean
        label: Customer-facing impact?
        when: "{{ or (eq .severity 'medium') (eq .severity 'critical') }}"
\end{verbatim}

\paragraph{Validation rules for \texttt{collector}}

\begin{itemize}
  \item A \texttt{collector} step \textbf{MUST} have at least one field in
        \texttt{fields}.  An empty \texttt{fields} array is a schema validation
        error.  Structural validation enforces \texttt{minItems: 1} on the
        \texttt{fields} array.
  \item Each field \texttt{name} must be a valid identifier (alphanumeric and
        underscore, must start with a letter).  Field names must be unique
        within a step.
  \item For \texttt{select} fields, exactly one of \texttt{options} or
        \texttt{options\_from} must be present.
  \item For \texttt{number} fields, if both \texttt{validation.min} and
        \texttt{validation.max} are present, \texttt{min} must be
        $\leq$ \texttt{max}.
  \item For \texttt{date} and \texttt{datetime} fields, if both
        \texttt{validation.min} and \texttt{validation.max} are present,
        \texttt{min} must be chronologically $\leq$ \texttt{max}.
  \item A field \texttt{when} expression \textbf{MUST NOT} reference a field
        declared later in the same \texttt{fields} array (no forward references).
        Forward references are detected at parse time and are a schema
        validation error.
  \item A \texttt{required: true} field with a \texttt{when} expression is
        only required when the \texttt{when} condition is truthy.  When
        \texttt{when} evaluates to falsy, \texttt{required} does not apply and
        the variable binding is skipped entirely.
  \item For \texttt{text} fields, if both \texttt{validation.min\_length} and
        \texttt{validation.max\_length} are present, \texttt{max\_length}
        \textbf{MUST} be strictly greater than \texttt{min\_length}.
  \item For \texttt{text} fields, \texttt{validation.pattern} \textbf{MUST}
        be a valid ECMA~262 regular expression; it is validated at schema
        load time and will cause a structural validation error if invalid.
  \item For \texttt{text} fields, \texttt{validation.pattern\_hint} is
        silently ignored when \texttt{validation.pattern} is absent
        (warning, not error).
  \item For \texttt{autocomplete} fields, \texttt{options\_from} \textbf{MUST}
        be present and \textbf{MUST} include both \texttt{provider} and
        \texttt{field}.  \texttt{options} (static list) is not valid on
        \texttt{autocomplete} fields and is a schema validation error.
  \item For \texttt{autocomplete} fields, \texttt{options\_from.min\_chars}
        \textbf{MUST} be $\geq 0$ when specified (default 2).
        \texttt{options\_from.debounce\_ms} \textbf{MUST} be $\geq 0$ (default 300).
  \item \texttt{validation.format} is only meaningful when \texttt{multiline:
        true} is also set.  If \texttt{format} is declared on a
        non-multiline field, a validation warning is emitted and the
        constraint is ignored at runtime.
  \item \texttt{validation.format: toml} is a validation warning in v2.0
        (TOML parse validation is deferred); the runbook is accepted and the
        format constraint is not enforced at runtime.
  \item A field with \texttt{ephemeral: true} and \texttt{type: file} or
        \texttt{type: image} is a validation \emph{warning} (not error).
        File payloads are large; writing \texttt{[REDACTED]} in the audit
        trail is acceptable, but authors should consider whether attachment
        evidence is appropriate alongside an ephemeral field.
\end{itemize}

% ─────────────────────────────────────────────────────────────────────────────
% Figure: Collector input triad — choice / decision / collector
% ─────────────────────────────────────────────────────────────────────────────
\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=1.0cm and 1.6cm, scale=0.85, transform shape]
  %% ── choice ──────────────────────────────────────────────────────────────────
  \node[catbox] (tri-choice-lbl) {\texttt{type: choice}};
  \node[stepbox, below=0.5cm of tri-choice-lbl] (tri-choice-q) {Prompt: pick\\one option};
  \node[stepbox, below=0.5cm of tri-choice-q]   (tri-choice-v) {Store selected\\value in var};
  \draw[gertarrow] (tri-choice-q) -- (tri-choice-v);
  \node[below=0.3cm of tri-choice-v, font=\scriptsize\itshape, color=black!55]
       {value-capture only};

  %% ── decision ────────────────────────────────────────────────────────────────
  \node[catbox, right=2.6cm of tri-choice-lbl] (tri-dec-lbl) {\texttt{type: decision}};
  \node[stepbox, below=0.5cm of tri-dec-lbl]   (tri-dec-q)   {Prompt: pick\\a path};
  \node[stepbox, below left=0.5cm and 0.5cm  of tri-dec-q] (tri-dec-p1) {Branch A\\steps};
  \node[stepbox, below right=0.5cm and 0.5cm of tri-dec-q] (tri-dec-p2) {Branch B\\steps};
  \draw[gertarrow] (tri-dec-q) -- (tri-dec-p1);
  \draw[gertarrow] (tri-dec-q) -- (tri-dec-p2);
  \node[below=1.05cm of tri-dec-q, font=\scriptsize\itshape, color=black!55]
       {control-flow fork};

  %% ── collector ───────────────────────────────────────────────────────────────
  \node[catbox, right=2.6cm of tri-dec-lbl] (tri-col-lbl) {\texttt{type: collector}};
  \node[stepbox, below=0.5cm of tri-col-lbl] (tri-col-form) {Present all\\fields at once};
  \node[stepbox, below=0.5cm of tri-col-form] (tri-col-val) {Validate \&\\store each field};
  \draw[gertarrow] (tri-col-form) -- (tri-col-val);
  \node[below=0.3cm of tri-col-val, font=\scriptsize\itshape, color=black!55]
       {structured multi-field input};

  %% ── vertical dividers ───────────────────────────────────────────────────────
  \begin{scope}[on background layer]
    \draw[dashed, black!25] ([xshift=1.3cm]tri-choice-lbl.north east) --
                            ([xshift=1.3cm]tri-choice-v.south east);
    \draw[dashed, black!25] ([xshift=1.3cm]tri-dec-lbl.north east) --
                            ([xshift=1.3cm, yshift=-1.6cm]tri-dec-lbl.south east);
  \end{scope}
\end{tikzpicture}
\caption{Human-input step triad: \texttt{choice} captures a single selection as a value; \texttt{decision} forks execution to the chosen branch; \texttt{collector} presents multiple typed fields atomically and stores all captured values.}
\label{fig:collector-triad}
\end{figure}

\subsection{Step Type: approve}
\label{subsec:step-approve}

An \texttt{approve} step is a dedicated, standalone approval gate that suspends
execution until one or more authorized roles explicitly approve (or until the
configured timeout fires).  Unlike the inline \texttt{approvals} block available
on \texttt{collector} and \texttt{choice} steps, \texttt{approve} carries no
data-collection fields --- its sole purpose is to capture an authoritative
go/no-go decision and record it in the run audit trail.

\begin{center}
\begin{tabular}{lllp{4.8cm}}
\hline
\textbf{Field} & \textbf{Type} & \textbf{Required} & \textbf{Description} \\
\hline
\texttt{title}                    & string  & No  & Human-readable approval prompt \\
\texttt{approvals}                & object  & Yes & Approval gate config (see below) \\
\texttt{timeout}                  & string  & No  & Wall-clock duration (e.g.\ \texttt{48h}); mutually exclusive with \texttt{timeout\_business\_days} \\
\texttt{timeout\_business\_days}  & integer & No  & Timeout in business days; mutually exclusive with \texttt{timeout}; requires \texttt{timezone} \\
\texttt{timezone}                 & string  & No  & IANA timezone (e.g.\ \texttt{America/New\_York}); required when \texttt{timeout\_business\_days} is set \\
\texttt{business\_calendar}       & string  & No  & Named calendar ID (e.g.\ \texttt{us-federal}, \texttt{uk-banking}); defaults to Mon--Fri with no holidays \\
\texttt{on\_timeout}              & string  & No  & \texttt{fail}, \texttt{continue}, or \texttt{branch:<id>} \\
\texttt{contract}                 & object  & No  & Behavioral contract for governance \\
\hline
\end{tabular}
\end{center}

\paragraph{approvals block}

The \texttt{approvals} object controls who may approve and the quorum semantics:

\begin{center}
\begin{tabular}{lllp{4.8cm}}
\hline
\textbf{Field} & \textbf{Type} & \textbf{Required} & \textbf{Description} \\
\hline
\texttt{mode}     & string   & No  & \texttt{all} (default) --- all listed roles must approve; \texttt{any} --- one approval suffices; \texttt{quorum} --- M-of-N from \texttt{pool} \\
\texttt{roles}    & string[] & No  & Authorized role names; used with \texttt{mode: all} or \texttt{mode: any}; mutually exclusive with \texttt{pool} \\
\texttt{pool}     & string[] & No  & Eligible role names for quorum; used with \texttt{mode: quorum}; mutually exclusive with \texttt{roles} \\
\texttt{required} & integer  & No  & Number of approvals required from \texttt{pool}; used with \texttt{mode: quorum}; must be $\geq 1$ and $\leq$ \texttt{len(pool)} \\
\hline
\end{tabular}
\end{center}

\paragraph{Business-Day Timeout}

\texttt{timeout\_business\_days: 3} means the approval must be granted within
3 working days from the moment the \texttt{approve} step became active.  The
runtime counts elapsed business days using the following rules:

\begin{itemize}
  \item The clock starts when the step enters the \texttt{waiting} state.
  \item Weekends (Saturday and Sunday) do not count toward the timeout.
  \item Holidays defined in the named \texttt{business\_calendar} do not count.
  \item If \texttt{business\_calendar} is omitted, the calendar defaults to
        Monday--Friday with no holidays.
  \item \texttt{timezone} determines the midnight boundary used for day counting;
        it is required whenever \texttt{timeout\_business\_days} is set.
\end{itemize}

If both \texttt{timeout} and \texttt{timeout\_business\_days} are present in the
same step, schema validation \textbf{MUST} reject the runbook with a structural
error before execution begins.  The \texttt{on\_timeout} semantics remain
unchanged: \texttt{fail} terminates the run, \texttt{continue} advances to the
next step, and \texttt{branch:<id>} jumps to the named branch.

\begin{minted}{yaml}
- step:
    id: legal_review
    type: approve
    title: "Legal team approval"
    approvals:
      roles: [legal-counsel]
    timeout_business_days: 5
    timezone: "America/New_York"
    business_calendar: us-federal
    on_timeout: branch:escalate_legal
    contract:
      effects: [approval.legal]
      reads: []
      writes: []
\end{minted}

\paragraph{Quorum Approval}

Setting \texttt{mode: quorum} together with \texttt{pool} and \texttt{required}
enables M-of-N approval semantics:

\begin{itemize}
  \item The runtime accepts approvals from any role listed in \texttt{pool}.
  \item Once the number of distinct approvals reaches \texttt{required}, the step
        proceeds immediately without waiting for remaining pool members.
  \item Each pool member may approve only once; their identity (role + principal)
        is recorded in the run audit trail.
  \item If a pool member explicitly rejects, the rejection is recorded in the
        audit trail.  A rejection does not by itself block the step --- the step
        is blocked only if so many members have rejected that the remaining
        approvers can no longer reach \texttt{required}.  (For example, if
        \texttt{required: 2} and \texttt{pool} has 3 members and 2 reject, the
        step immediately fails because quorum is no longer reachable.)
\end{itemize}

\begin{minted}{yaml}
- step:
    id: security_quorum
    type: approve
    title: "Security committee approval (2 of 5)"
    approvals:
      mode: quorum
      pool: [security-lead, ciso, security-eng-1,
             security-eng-2, security-eng-3]
      required: 2
    timeout_business_days: 3
    timezone: "Europe/London"
    on_timeout: fail
    contract:
      effects: [approval.security]
      reads: []
      writes: []
\end{minted}

\paragraph{Validation Rules}

The following conditions are schema validation errors that \textbf{MUST} be
reported before execution:

\begin{enumerate}
  \item \texttt{timeout} and \texttt{timeout\_business\_days} are mutually
        exclusive; both present is an error.
  \item \texttt{timeout\_business\_days} requires \texttt{timezone}; omitting
        \texttt{timezone} when \texttt{timeout\_business\_days} is set is an
        error.
  \item \texttt{approvals.mode: quorum} requires both \texttt{approvals.pool}
        and \texttt{approvals.required}; either missing is an error.
  \item \texttt{approvals.required} must satisfy $1 \leq \texttt{required} \leq
        \texttt{len(pool)}$; violating this range is an error.
  \item \texttt{approvals.roles} and \texttt{approvals.pool} are mutually
        exclusive; both present is an error.
  \item \texttt{approvals.mode: all} or \texttt{mode: any} requires
        \texttt{approvals.roles} (not \texttt{pool}); using \texttt{pool} with
        these modes is an error.
\end{enumerate}

\subsection{Step Type: tool}
\label{subsec:step-tool}

Invokes a named tool action.  The tool must be declared in \texttt{toolRefs}.
The runtime dispatches to the tool's configured transport (stdio, jsonrpc, or
mcp).

\begin{center}
\begin{tabular}{lllp{5cm}}
\hline
\textbf{Field} & \textbf{Type} & \textbf{Required} & \textbf{Description} \\
\hline
\texttt{tool.name}    & string & Yes & Tool name (must match toolRefs entry) \\
\texttt{tool.action}  & string & Yes & Action declared in tool definition \\
\texttt{tool.args}    & map    & No  & Argument key-value map (templates expanded) \\
\texttt{tool.version} & string & No  & Optional version pin \\
\texttt{capture}      & map    & No  & \texttt{stdout | stderr | json.<path>} → var \\
\hline
\end{tabular}
\end{center}

Output capture in v2 adds a \texttt{json.<path>} form for JSON-structured tool
outputs, eliminating the need for follow-up \texttt{cli} steps with \texttt{jq}:

\begin{minted}{yaml}
capture:
  pod_name: json.items[0].metadata.name
  status:   json.items[0].status.phase
\end{minted}

\begin{verbatim}
- step:
    id: list_pods
    type: tool
    title: "List pods in {{ .namespace }}"
    tool:
      name: kubectl
      action: get-pods
      args:
        namespace: "{{ .namespace }}"
        output: json
    capture:
      pod_count: json.items | length
      first_pod: json.items[0].metadata.name
    contract:
      effects: [k8s.read]
      reads: [namespace]
      writes: [pod_count, first_pod]
\end{verbatim}

% ─────────────────────────────────────────────────────────────────────────────
% Figure: Tool invocation flow
% ─────────────────────────────────────────────────────────────────────────────
\begin{figure}[H]
\centering
\resizebox{\linewidth}{!}{%
\begin{tikzpicture}[node distance=0.5cm and 0.9cm, scale=0.85, transform shape]
  \node[stepbox] (ti-resolve)  {Resolve Tool\\from toolRefs};
  \node[stepbox, right=of ti-resolve]  (ti-transport) {Select Transport\\(stdio/jsonrpc/mcp)};
  \node[stepbox, right=of ti-transport] (ti-auth)     {Authenticate\\(if required)};
  \node[stepbox, right=of ti-auth]     (ti-invoke)    {Invoke Action\\with args};
  \node[stepbox, right=of ti-invoke]   (ti-stream)    {Stream /\\Await Result};
  \node[stepbox, right=of ti-stream]   (ti-capture)   {Capture Output\\to variables};

  %% Failure branch
  \node[stepbox, below=1.2cm of ti-resolve, fill=black!10] (ti-notfound) {FAILED\\(tool not found)};
  \node[stepbox, below=1.2cm of ti-invoke,  fill=black!10] (ti-invokerr) {FAILED\\(invocation error)};
  \node[stepbox, below=1.2cm of ti-capture, fill=black!3]  (ti-pass)     {PASSED};

  %% Main flow
  \draw[gertarrow] (ti-resolve)   -- (ti-transport);
  \draw[gertarrow] (ti-transport) -- (ti-auth);
  \draw[gertarrow] (ti-auth)      -- (ti-invoke);
  \draw[gertarrow] (ti-invoke)    -- (ti-stream);
  \draw[gertarrow] (ti-stream)    -- (ti-capture);

  %% Branches
  \draw[gertarrow] (ti-resolve) -- node[left,  font=\scriptsize\itshape] {not found} (ti-notfound);
  \draw[gertarrow] (ti-invoke)  -- node[left,  font=\scriptsize\itshape] {error}     (ti-invokerr);
  \draw[gertarrow] (ti-capture) -- node[left,  font=\scriptsize\itshape] {ok}        (ti-pass);
\end{tikzpicture}}
\caption{\texttt{type: tool} invocation flow: tool resolution, transport selection, authentication, action invocation, result streaming, and output capture.  A missing tool or invocation error terminates the step as \textsc{failed}.}
\label{fig:tool-invocation-flow}
\end{figure}

\subsection{Step Type: wait\_for\_event}
\label{subsec:step-wait-for-event}

Pauses runbook execution until an inbound event arrives on a declared source.
Use this step type whenever a runbook must suspend and wait for an external
signal before proceeding — a Prometheus alert webhook, an approval callback
from a ticketing system, a SIEM event, a message-queue message, or an
OS/process signal.

When the runtime reaches a \texttt{wait\_for\_event} step it:
\begin{enumerate}
  \item Opens the declared inbound endpoint (HTTP listener, queue consumer, or
        internal channel) for the lifetime of the wait.
  \item Suspends the run record in state \texttt{waiting\_for\_event}.
  \item When a matching event arrives and passes any declared \texttt{event.filter}
        and \texttt{event.payload\_schema} checks, it captures payload fields into
        run variables (via \texttt{capture}) and resumes execution at the next step.
  \item If the \texttt{timeout} expires before a matching event arrives, the step
        behaves according to \texttt{on\_timeout}: \texttt{fail} (default),
        \texttt{continue} (skip and proceed), or
        \texttt{branch:<step-id>} (jump to the named step).
\end{enumerate}

\begin{center}
\begin{tabular}{lllp{5cm}}
\hline
\textbf{Field} & \textbf{Type} & \textbf{Required} & \textbf{Description} \\
\hline
\texttt{event.source}         & string & Yes & Event source type: \texttt{webhook}, \texttt{message}, \texttt{signal}, or \texttt{channel} \\
\texttt{event.id}             & string & No  & Logical event name or channel (e.g.\ \texttt{prometheus.alert.firing}); templates expanded \\
\texttt{event.filter}         & map    & No  & Key-value pairs that must match in the incoming event payload \\
\texttt{event.payload\_schema} & string & No  & JSON~Schema URL to validate the incoming payload before capture \\
\texttt{capture}              & map    & No  & \texttt{json.<path>} → variable; maps event payload fields to run variables \\
\texttt{timeout}              & string & No  & Wall-clock duration (e.g.\ \texttt{30m}, \texttt{2h}); default: no timeout \\
\texttt{on\_timeout}          & string & No  & \texttt{fail} (default), \texttt{continue}, or \texttt{branch:<step-id>} \\
\hline
\end{tabular}
\end{center}

\paragraph{Example 1 — Prometheus alert webhook}

\begin{minted}{yaml}
- step:
    id: wait_prometheus_alert
    type: wait_for_event
    title: "Wait for Prometheus alert to fire"
    event:
      source: webhook
      id: prometheus.alert.firing
      filter:
        alertname: "HighErrorRate"
        severity: critical
    capture:
      alert_value: json.commonAnnotations.value
      alert_labels: json.commonLabels
    timeout: 30m
    on_timeout: fail
    contract:
      effects: []
      reads: []
      writes: [alert_value, alert_labels]
\end{minted}

\paragraph{Example 2 — Named channel / approval callback}

\begin{verbatim}
- step:
    id: wait_approval_callback
    type: wait_for_event
    title: "Wait for external approval system callback"
    event:
      source: channel
      id: "approval.{{ .request_id }}"
    capture:
      approved_by: json.approver
      decision: json.decision
    timeout: 48h
    on_timeout: branch:escalate_approval
    contract:
      effects: []
      reads: [request_id]
      writes: [approved_by, decision]
\end{verbatim}

\paragraph{Transport Note}

The runtime is responsible for exposing the inbound endpoint; the runbook schema
only declares intent.  The specific endpoint URL or address is runtime-determined
and available to subsequent steps as \texttt{\{\{\ .gert.event.<id>.endpoint\ \}\}}.
Event source semantics:
\begin{itemize}
  \item \texttt{webhook} — runtime opens an HTTP~POST listener; the sender POSTs a JSON body.
  \item \texttt{message} — runtime subscribes to a message-queue topic or queue name
        given by \texttt{event.id}.
  \item \texttt{signal} — runtime registers a named OS or process signal handler.
  \item \texttt{channel} — runtime uses a gert-internal named channel, enabling
        runbook-to-runbook or tool-to-runbook event delivery.
\end{itemize}

\paragraph{Security Note}

Webhook endpoints are authenticated per run.  The runtime generates a one-time
HMAC token when the endpoint is opened; the token is available as
\texttt{\{\{\ .gert.event.<id>.token\ \}\}} for use in configuring the sending
system (e.g.\ embedding in a Prometheus alertmanager receiver URL).  The token
is single-use and expires when the endpoint closes (on resume or timeout).

\subsection{Step Type: include}
\label{subsec:step-include}

Expands another runbook's steps \emph{inline} into the current run path at the
point of the \texttt{include} step.  This is \textbf{not} a sub-procedure call:
the included runbook shares the caller's variable space entirely — no isolated
scope, no input/output mapping.  All included steps appear in the caller's
execution trace and audit log as if they were defined inline.

The \texttt{include.with} map injects variable overrides into the shared scope
before expansion; these values are immediately visible to the included steps and
to any steps that follow in the caller.

\begin{center}
\begin{tabular}{lllp{5.5cm}}
\hline
\textbf{Field} & \textbf{Type} & \textbf{Required} & \textbf{Description} \\
\hline
\texttt{include.runbook}  & string & Yes & Path or ID of the runbook to include. Relative paths resolve from the including runbook's location. \\
\texttt{include.with}     & map    & No  & Variable overrides injected into the shared scope before expansion (key: variable name, value: expression). Applied in order, visible to included steps. \\
\texttt{include.when}     & string & No  & Boolean expression. If false, the include is skipped entirely (no steps expanded). \\
\hline
\end{tabular}
\end{center}

\begin{verbatim}
- step:
    id: run_db_checks
    type: include
    title: "Run database pre-flight checks"
    include:
      runbook: ./shared/db-preflight.runbook.yaml
      with:
        db_host: "{{ .target_db }}"
        check_mode: read-only
    contract:
      effects: [db.read]
      reads: [target_db]
      writes: []
\end{verbatim}

\paragraph{Cycle Detection}
The runtime builds a runbook dependency graph at load time by resolving all
\texttt{include.runbook} references transitively.  A cycle exists when a runbook
directly or transitively includes itself.  Cyclic includes are a hard validation
error; the runtime rejects the runbook set and reports the full include chain:

\begin{verbatim}
cyclic include detected: A -> B -> A
\end{verbatim}

The validator reports every edge in the offending cycle so the operator can
identify the exact chain.

\paragraph{Difference from Sub-Procedure Call}
\texttt{type: include} shares the caller's variable space; there is no input
mapping and no output extraction.  The included steps appear inline in the run
trace — there is no sub-run, no separate audit entry, and no scope boundary.

If isolation \emph{is} needed (separate variable scope, explicit input/output
mapping), that is the responsibility of a future step type
(\texttt{type: call} or \texttt{type: delegate}).  That capability is
\textbf{not in v2.0 scope}.

% ─────────────────────────────────────────────────────────────────────────────
% Figure: Include inlining
% ─────────────────────────────────────────────────────────────────────────────
\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=0.6cm and 1.2cm, scale=0.85, transform shape]
  %% ── Parent runbook ──────────────────────────────────────────────────────────
  \node[stepbox] (inc-s1)      {Step 1\\(parent)};
  \node[stepbox, below=of inc-s1] (inc-inc) {\texttt{type: include}};
  \node[stepbox, below=of inc-inc] (inc-s3) {Step 3\\(parent)};

  \draw[gertarrow] (inc-s1)  -- (inc-inc);
  \draw[gertarrow] (inc-inc) -- (inc-s3);

  %% ── Child runbook (inlined) ──────────────────────────────────────────────────
  \node[stepbox, right=2.4cm of inc-inc] (inc-c1) {Child Step A};
  \node[stepbox, below=0.5cm of inc-c1] (inc-c2) {Child Step B};
  \draw[gertarrow] (inc-c1) -- (inc-c2);

  %% ── Dashed box around inlined content ────────────────────────────────────────
  \begin{scope}[on background layer]
    \node[gertfit, fit=(inc-c1)(inc-c2)] (inc-box) {};
  \end{scope}
  \node[above=2pt of inc-box, font=\scriptsize\sffamily\itshape, color=black!55]
       {inlined from child runbook (shared scope)};

  %% ── Expansion arrows ─────────────────────────────────────────────────────────
  \draw[gertarrow] (inc-inc) -- node[above, font=\scriptsize\itshape] {expand} (inc-c1);
  \draw[gertarrow] (inc-c2)  -- node[below, font=\scriptsize\itshape] {continue} (inc-s3);
\end{tikzpicture}
\caption{\texttt{type: include} inlines the child runbook's steps into the parent's execution path. All included steps share the parent variable scope; no sub-run boundary is created.}
\label{fig:include-inlining}
\end{figure}

\subsection{Step Type: branch}
\label{subsec:step-branch}

Evaluates a list of conditions and executes the first matching arm.  In v2,
\texttt{branch} is an explicit first-class step type (as well as the inline
\texttt{branches:} sibling on any step node).

\begin{center}
\begin{tabular}{lllp{5cm}}
\hline
\textbf{Field} & \textbf{Type} & \textbf{Required} & \textbf{Description} \\
\hline
\texttt{branches}           & array  & Yes & List of Branch objects \\
\texttt{branches[].condition} & string & Yes & Go template evaluating to \texttt{true}/\texttt{false} \\
\texttt{branches[].label}   & string & No  & Display label for diagram/trace \\
\texttt{branches[].steps}   & array  & No  & Steps to execute if condition matches \\
\hline
\end{tabular}
\end{center}

At most one branch arm executes.  If no arm matches and no arm has an
\texttt{else: true} marker, execution continues past the branch node with no
steps run.  The \texttt{else: true} marker (new in v2) designates a catch-all
arm:

\begin{verbatim}
- step:
    id: route_on_env
    type: branch
    title: Route by environment
    branches:
      - condition: '{{ eq .environment "production" }}'
        label: Production path
        steps:
          - step:
              id: require_approval
              type: manual
              title: Require change-manager approval
              approvals:
                min: 1
                roles: [change-manager]
      - condition: '{{ eq .environment "staging" }}'
        label: Staging path
        steps:
          - step:
              id: log_staging
              type: cli
              title: Log staging deploy
              command: echo
              args: ["Deploying to staging"]
      - else: true
        label: Unknown environment — abort
        steps:
          - step:
              id: abort
              type: end
              title: Unknown environment
              outcome:
                category: escalated
                code: unknown_env
\end{verbatim}

% ─────────────────────────────────────────────────────────────────────────────
% Figure: Branch logic
% ─────────────────────────────────────────────────────────────────────────────
\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=0.7cm and 1.6cm, scale=0.85, transform shape]
  %% ── Entry ────────────────────────────────────────────────────────────────────
  \node[stepbox] (br-entry) {Evaluate\\Conditions};

  %% ── Decision diamond ─────────────────────────────────────────────────────────
  \node[diamond, draw=black!70, fill=black!5, aspect=2.5,
        below=0.8cm of br-entry, font=\small, align=center] (br-dia) {arm match?};
  \draw[gertarrow] (br-entry) -- (br-dia);

  %% ── True branch ──────────────────────────────────────────────────────────────
  \node[stepbox, below left=1.0cm and 1.8cm of br-dia]  (br-arm1) {Arm 1\\steps};
  \node[stepbox, below=0.5cm of br-arm1]                (br-arm2) {Arm 2\\steps};
  \draw[gertarrow] (br-dia)  -| node[above left, font=\scriptsize\itshape, pos=0.3]
       {first match} (br-arm1);

  %% ── else / no-match ──────────────────────────────────────────────────────────
  \node[stepbox, below right=1.0cm and 1.8cm of br-dia] (br-else) {\texttt{else: true}\\arm (optional)};
  \draw[gertarrow] (br-dia) -| node[above right, font=\scriptsize\itshape, pos=0.3]
       {no match} (br-else);

  %% ── Merge ────────────────────────────────────────────────────────────────────
  \node[stepbox, below=3.0cm of br-dia] (br-cont) {Continue\\execution};
  \draw[gertarrow] (br-arm1) |- (br-cont);
  \draw[gertarrow] (br-arm2) |- (br-cont);
  \draw[gertarrow] (br-else) |- (br-cont);
\end{tikzpicture}
\caption{\texttt{type: branch} evaluates each arm's condition in order and executes the first matching arm. At most one arm runs. If no arm matches and no \texttt{else} arm is defined, execution continues with no steps run.}
\label{fig:branch-logic}
\end{figure}

\subsection{Step Type: iterate}
\label{subsec:step-iterate}

Repeats a block of steps over a collection or until a convergence condition
is met.  In v2 \texttt{iterate} is a TreeNode sibling (not a step type in the
\texttt{step.type} enum) — it appears at the tree level alongside \texttt{step}
nodes.

\begin{center}
\begin{tabular}{lllp{5cm}}
\hline
\textbf{Field} & \textbf{Type} & \textbf{Required} & \textbf{Description} \\
\hline
\texttt{iterate.over}  & string & * & Template resolving to comma-separated list \\
\texttt{iterate.as}    & string & No & Loop variable name (default: \texttt{item}) \\
\texttt{iterate.max}   & int    & * & Max iterations (convergence mode) \\
\texttt{iterate.until} & string & * & Convergence condition expression \\
\texttt{iterate.collect} & map  & No & Accumulate captures across passes \\
\texttt{iterate.steps} & array  & Yes & Steps to execute each pass \\
\hline
\multicolumn{4}{l}{* \texttt{over} or (\texttt{max} + \texttt{until}) required; mutually exclusive.} \\
\end{tabular}
\end{center}

The loop variable \texttt{\{\{\ .iteration\ \}\}} (0-indexed) is always available.
In list mode, \texttt{\{\{\ .item\ \}\}} (or the \texttt{as} name) holds the
current element.

\begin{verbatim}
# List iteration
- iterate:
    id: check_all_services
    over: "{{ .service_list }}"   # e.g. "api,worker,scheduler"
    as: svc
    collect:
      results: "{{ .svc }}:{{ .http_result }}"
    steps:
      - step:
          id: probe
          type: tool
          title: "Check {{ .svc }}"
          tool:
            name: curl
            action: head
            args:
              url: "https://{{ .svc }}.internal/healthz"
          capture:
            http_result: stdout

# Convergence iteration
- iterate:
    id: wait_for_deploy
    max: 10
    until: '{{ eq .deploy_status "Running" }}'
    steps:
      - step:
          id: poll_deploy
          type: tool
          title: "Poll deployment (pass {{ .iteration }})"
          tool:
            name: kubectl
            action: get-deploy-status
            args:
              name: "{{ .deployment }}"
          capture:
            deploy_status: json.status.phase
      - step:
          id: wait
          type: cli
          title: Wait 15s
          command: sleep
          args: ["15"]
\end{verbatim}

% ─────────────────────────────────────────────────────────────────────────────
% Figure: Iterate loop
% ─────────────────────────────────────────────────────────────────────────────
\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=0.7cm and 1.4cm, scale=0.85, transform shape]
  %% ── Nodes ────────────────────────────────────────────────────────────────────
  \node[stepbox] (it-init)  {Initialize\\Iterator};
  \node[stepbox, below=of it-init]  (it-cond)  {Test Condition\\(or items remain?)};
  \node[stepbox, below=of it-cond]  (it-body)  {Execute Body\\Steps};
  \node[stepbox, below=of it-body]  (it-incr)  {Increment /\\Advance Item};
  \node[stepbox, right=2.2cm of it-cond, fill=black!3] (it-exit) {Exit Loop\\(continue run)};
  \node[stepbox, below right=0.5cm and 2.2cm of it-cond, fill=black!10] (it-maxerr) {FAILED\\(max exceeded)};

  %% ── Arrows ────────────────────────────────────────────────────────────────────
  \draw[gertarrow] (it-init) -- (it-cond);
  \draw[gertarrow] (it-cond) -- node[left, font=\scriptsize\itshape] {true / has item} (it-body);
  \draw[gertarrow] (it-body) -- (it-incr);
  \draw[gertarrow] (it-cond) -- node[above, font=\scriptsize\itshape] {false / exhausted} (it-exit);

  %% Loop back arrow
  \draw[gertarrow] (it-incr.east) -- ++(1.2,0) |-
       node[right, font=\scriptsize\itshape, pos=0.25] {next pass} (it-cond.east);

  %% Max exceeded
  \draw[gertarrow] (it-cond.east) -- node[right, font=\scriptsize\itshape, pos=0.6]
       {max reached} (it-maxerr);
\end{tikzpicture}
\caption{\texttt{type: iterate} loop: the condition (items remaining or \texttt{until} expression) is tested before each pass. The body executes, the iterator advances, and the loop repeats until the condition is false, items are exhausted, or \texttt{max} iterations are reached.}
\label{fig:iterate-loop}
\end{figure}

\subsection{Step Type: parallel}
\label{subsec:step-parallel}

Executes a set of independent step groups concurrently, then joins.  New in v2,
addressing the fan-out/fan-in gap identified in Dennis's research brief.

\begin{center}
\begin{tabular}{lllp{5cm}}
\hline
\textbf{Field} & \textbf{Type} & \textbf{Required} & \textbf{Description} \\
\hline
\texttt{branches}          & array  & Yes & Each branch is an independent group \\
\texttt{join.wait\_for}    & enum   & No  & \texttt{all | any | majority} (default: \texttt{all}) \\
\texttt{join.on\_failure}  & enum   & No  & \texttt{fail | continue} (default: \texttt{fail}) \\
\hline
\end{tabular}
\end{center}

Parallel branches are isolated from each other's variable writes during
execution.  After the join, all \texttt{capture} values from all branches are
merged into the caller's variable map.  Write conflicts (two branches writing the
same variable) are resolved by last-writer-wins and a warning is emitted.

\begin{verbatim}
- step:
    id: parallel_checks
    type: parallel
    title: Run DNS and HTTP checks concurrently
    branches:
      - label: DNS check
        steps:
          - step:
              id: dns_check
              type: tool
              tool:
                name: nslookup
                action: lookup
                args:
                  host: "{{ .hostname }}"
              capture:
                dns_result: stdout
      - label: HTTP check
        steps:
          - step:
              id: http_check
              type: tool
              tool:
                name: curl
                action: head
                args:
                  url: "https://{{ .hostname }}/healthz"
              capture:
                http_result: stdout
    join:
      wait_for: all
      on_failure: fail
\end{verbatim}

% ─────────────────────────────────────────────────────────────────────────────
% Figure: Parallel fan-out / fan-in
% ─────────────────────────────────────────────────────────────────────────────
\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=0.6cm and 1.0cm, scale=0.85, transform shape]
  %% ── Fork ─────────────────────────────────────────────────────────────────────
  \node[stepbox] (par-fork) {Fork\\(dispatch all)};

  %% ── Branches ─────────────────────────────────────────────────────────────────
  \node[stepbox, below left=1.2cm and 2.2cm of par-fork]  (par-b1) {Branch 1\\steps\ldots};
  \node[stepbox, below=1.2cm of par-fork]                 (par-b2) {Branch 2\\steps\ldots};
  \node[stepbox, below right=1.2cm and 2.2cm of par-fork] (par-b3) {Branch N\\steps\ldots};

  \draw[gertarrow] (par-fork) -- (par-b1);
  \draw[gertarrow] (par-fork) -- (par-b2);
  \draw[gertarrow] (par-fork) -- (par-b3);

  %% ── Join ─────────────────────────────────────────────────────────────────────
  \node[stepbox, below=2.8cm of par-fork] (par-join) {Join\\(\texttt{all | any | majority})};

  \draw[gertarrow] (par-b1) -- (par-join);
  \draw[gertarrow] (par-b2) -- (par-join);
  \draw[gertarrow] (par-b3) -- (par-join);

  %% ── Merge variables ──────────────────────────────────────────────────────────
  \node[stepbox, below=0.7cm of par-join, fill=black!3] (par-merge) {Merge captured\\variables};
  \draw[gertarrow] (par-join) -- (par-merge);

  %% ── Failure bypass ───────────────────────────────────────────────────────────
  \node[stepbox, right=2.4cm of par-join, fill=black!10] (par-fail) {FAILED\\(\texttt{on\_failure: fail})};
  \draw[gertarrow, dashed] (par-join) -- node[above, font=\scriptsize\itshape]
       {branch failed} (par-fail);
\end{tikzpicture}
\caption{\texttt{type: parallel} fan-out/fan-in: all branches are dispatched concurrently. The join node waits for the configured quorum (\texttt{all}, \texttt{any}, or \texttt{majority}), then merges captured variables. A branch failure triggers the \texttt{on\_failure} policy.}
\label{fig:parallel-fanout}
\end{figure}

\subsection{Step Type: assert}
\label{subsec:step-assert}

Evaluates a list of assertions against captured variables.  Fails the run if
any assertion does not pass.

\begin{verbatim}
- step:
    id: verify_deployment
    type: assert
    title: Verify deployment succeeded
    assert:
      - type: contains
        subject: "{{ .deploy_output }}"
        expected: "successfully rolled out"
      - type: eq
        subject: "{{ .exit_code }}"
        expected: "0"
      - type: json_path
        subject: "{{ .api_response }}"
        path: "$.status"
        expected: "active"
\end{verbatim}

Assertion types: \texttt{contains}, \texttt{not\_contains}, \texttt{matches}
(regex), \texttt{eq}, \texttt{ne}, \texttt{lt}, \texttt{gt}, \texttt{exit\_code},
\texttt{json\_path}.

\subsection{Step Type: compensate}
\label{subsec:step-compensate}

New in v2.  Registers a compensation (rollback) action that is executed in
reverse order if the run fails or is cancelled after this step.  Implements the
saga pattern for long-running operational transactions.

\begin{center}
\begin{tabular}{lllp{5cm}}
\hline
\textbf{Field} & \textbf{Type} & \textbf{Required} & \textbf{Description} \\
\hline
\texttt{compensate.on}   & enum  & No  & \texttt{failure | cancel | any} (default: \texttt{failure}) \\
\texttt{compensate.steps}& array & Yes & Steps to execute as compensation \\
\hline
\end{tabular}
\end{center}

The compensation steps execute in LIFO order relative to the sequence of
\texttt{compensate} nodes encountered during the forward execution pass.
Compensation steps share the same variable context as the point at which the
\texttt{compensate} node was registered.

\begin{verbatim}
- step:
    id: scale_down
    type: tool
    title: Scale down service
    tool:
      name: kubectl
      action: scale
      args:
        deployment: "{{ .deployment }}"
        replicas: "0"

- step:
    id: register_scale_up
    type: compensate
    title: Compensation — scale back up on failure
    compensate:
      on: failure
      steps:
        - step:
            id: scale_up_comp
            type: tool
            title: Restore service scale
            tool:
              name: kubectl
              action: scale
              args:
                deployment: "{{ .deployment }}"
                replicas: "{{ .original_replicas }}"
\end{verbatim}

% ─────────────────────────────────────────────────────────────────────────────
% Figure: Compensation (saga rollback) flow
% ─────────────────────────────────────────────────────────────────────────────
\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=0.6cm and 1.3cm, scale=0.85, transform shape]
  %% ── Forward execution ────────────────────────────────────────────────────────
  \node[stepbox] (cmp-s1)       {Step A\\(\texttt{compensate} registered)};
  \node[stepbox, right=of cmp-s1]  (cmp-s2) {Step B\\(\texttt{compensate} registered)};
  \node[stepbox, right=of cmp-s2]  (cmp-s3) {Step C\\(failure point)};

  \draw[gertarrow] (cmp-s1) -- (cmp-s2);
  \draw[gertarrow] (cmp-s2) -- (cmp-s3);

  %% ── Failure trigger ──────────────────────────────────────────────────────────
  \node[stepbox, below=1.3cm of cmp-s3, fill=black!10] (cmp-fail) {FAILED /\\CANCELLED};
  \draw[gertarrow] (cmp-s3) -- node[right, font=\scriptsize\itshape] {failure} (cmp-fail);

  %% ── Compensation reverse execution ───────────────────────────────────────────
  \node[stepbox, below=1.3cm of cmp-s2] (cmp-undo-b) {Compensate B\\(reverse order)};
  \node[stepbox, below=1.3cm of cmp-s1] (cmp-undo-a) {Compensate A\\(reverse order)};

  \draw[gertarrow] (cmp-fail)   -- node[above, font=\scriptsize\itshape] {trigger} (cmp-undo-b);
  \draw[gertarrow] (cmp-undo-b) -- node[above, font=\scriptsize\itshape] {then}    (cmp-undo-a);

  %% ── Annotation ────────────────────────────────────────────────────────────────
  \node[below=0.4cm of cmp-undo-a, font=\small\itshape, color=black!55]
       {saga pattern: undo in reverse registration order};
\end{tikzpicture}
\caption{\texttt{type: compensate} saga flow: each compensation step is registered during forward execution. On failure or cancellation, registered compensations fire in reverse order, implementing the saga pattern for long-running operational transactions.}
\label{fig:compensation-flow}
\end{figure}

\subsection{Step Type: end}
\label{subsec:step-end}

Declares a terminal outcome and halts execution.  Renamed from \texttt{router}
in v1.

\begin{verbatim}
- step:
    id: resolved
    type: end
    title: Incident resolved
    outcome:
      category: resolved       # resolved | escalated | no_action | needs_rca
      code: dns_healthy
      meta:
        resolution_time: "{{ .elapsed }}"
\end{verbatim}

% ============================================================
\section{Tool Definition Schema (v2)}
\label{sec:tool-schema}
% ============================================================

Tool definitions describe callable tools: their actions, argument schemas,
transport, and behavioral contract.  In v2 the \texttt{tool/v0} version is
replaced by \texttt{tool/v2}.

\subsection{Tool Definition Field Inventory}
\label{subsec:tool-fields}

\begin{verbatim}
$schema: "https://schemas.gert.dev/tool/v2.json"
apiVersion: tool/v2

meta:
  name: kubectl
  version: "1.2"
  description: Kubernetes CLI wrapper
  binary: kubectl

transport:
  mode: stdio         # stdio | jsonrpc | mcp
  # jsonrpc options:
  # start: ["node", "server.js"]
  # port: 3000
  # mcp options:
  # start: ["python", "mcp_server.py"]

actions:
  get-pods:
    description: List pods in a namespace
    argv: ["get", "pods", "-n", "{{ .namespace }}", "-o", "json"]
    args:
      namespace:
        type: string
        required: true
        description: Kubernetes namespace
      output:
        type: string
        required: false
        default: json
    capture:
      stdout:
        format: json    # json | text | lines
    contract:
      effects: [k8s.read]
      reads: [namespace]
    governance:
      action: allow
\end{verbatim}

\paragraph{New in v2}

\begin{itemize}
  \item \texttt{capture.stdout.format} — declares the output format; enables
        structured \texttt{json.<path>} capture in runbook steps.
  \item \texttt{actions.<name>.contract} — per-action behavioral contract
        automatically merged with any step-level contract override.
  \item \texttt{actions.<name>.governance} — per-action governance policy;
        overrides the runbook-level rules for this action only.
  \item \texttt{meta.version} — explicit tool version for auditing and pinning.
\end{itemize}

% ============================================================
\section{Provider Definition Schema (v2)}
\label{sec:provider-schema}
% ============================================================

Provider definitions declare external input resolution services.
In v2 the \texttt{provider/v0} version is replaced by \texttt{provider/v2}.

\begin{minted}{yaml}
$schema: "https://schemas.gert.dev/provider/v2.json"
apiVersion: provider/v2

meta:
  name: incident
  version: "1.0"
  description: PagerDuty incident context provider
  kind: input-provider    # input-provider | enrichment-provider

transport:
  mode: jsonrpc
  start: ["./providers/pagerduty-provider"]

fields:
  id:
    type: string
    description: Active incident ID
  title:
    type: string
    description: Incident title
  severity:
    type: string
    enum: [P1, P2, P3, P4]
  affected_service:
    type: string
    description: Primary impacted service name

capabilities:
  - input-resolution
\end{minted}

The \texttt{fields} map defines the provider's schema.  Each field name
corresponds to a \texttt{from: <provider>.<field>} binding in a runbook input.
The semantic validator checks that all \texttt{from: <provider>.*} bindings
reference a field declared in a loaded provider definition.

% ============================================================
\section{Namespace Convention for Extensions}
\label{sec:namespaces}
% ============================================================

\subsection{The Extension Field Problem}

Strict JSON Schema validation (\texttt{additionalProperties: false}) is a core
v2 design constraint: unknown fields in a runbook must be rejected, not silently
ignored.  At the same time, extensions must be able to attach metadata to steps,
runbooks, and tool definitions without forking the core schema.

\subsection{Convention: \texttt{x-<namespace>:} at the Field Level}

v2 adopts the \textbf{\texttt{x-<namespace>}} prefix convention used by OpenAPI
and GitHub Actions.  Extension fields must:

\begin{enumerate}
  \item Start with \texttt{x-}.
  \item Be followed by a namespace component (lowercase alphanumeric and hyphens,
        matching \texttt{[a-z][a-z0-9-]+}).
  \item Be declared in an \textbf{extension manifest} registered via
        \texttt{gert extension register} or in \texttt{gert.yaml}.
\end{enumerate}

\paragraph{At the runbook level}

\begin{minted}{yaml}
$schema: "https://schemas.gert.dev/runbook/v2.json"
apiVersion: runbook/v2
id: deploy-service
name: Deploy Service

x-myorg-cost-center: "engineering-ops"
x-myorg-change-id: "CHG-20260418-001"
x-otel-service-name: "payment-service"
\end{minted}

\paragraph{At the step level}

\begin{verbatim}
- step:
    id: deploy
    type: tool
    title: Deploy to production
    x-myorg-risk-level: high
    x-myorg-rollback-id: "{{ .deploy_id }}"
\end{verbatim}

\subsection{Extension Validation}

\begin{enumerate}
  \item \textbf{Unregistered extension fields} — The JSON~Schema validator
        accepts any field matching \texttt{\^{}x-[a-z][a-z0-9-]+\$} without
        requiring a registered schema.  This enables zero-friction annotation.
        The \texttt{gert validate} command emits a \texttt{WARNING} for each
        unregistered extension field.
  \item \textbf{Registered extension fields} — An extension may ship a JSON
        Schema fragment that constrains its field values.  Once registered, the
        validator applies the fragment and reports violations as errors, not
        warnings.
  \item \textbf{Unknown non-extension fields} — Any field that does not match
        a core field name and does not start with \texttt{x-} is a hard
        validation error (\texttt{additionalProperties: false}).
\end{enumerate}

\subsection{Extension Registration Format}

\begin{minted}{yaml}
# .gert/extensions/myorg.yaml
apiVersion: extension/v1
namespace: myorg
description: MyOrg internal runbook annotations

fields:
  cost-center:
    type: string
    description: Cost center code for billing
    pattern: "^[a-z]+-[a-z]+"
  risk-level:
    type: string
    enum: [low, medium, high, critical]
  change-id:
    type: string
    pattern: "^CHG-[0-9]{8}-[0-9]{3}$"
  rollback-id:
    type: string

applies-to: [runbook, step]
\end{minted}

The extension schema is merged into the live JSON~Schema when the extension is
registered, enabling IDE auto-completion for extension fields as well.

% ============================================================
\section{Structural vs.\ Semantic Validation}
\label{sec:validation}
% ============================================================

v2 validation runs in two sequential phases.  Both must pass for a runbook to
be considered valid.

\subsection{Phase 1: Structural Validation}
\label{subsec:structural}

Structural validation is pure JSON~Schema~Draft~2020-12 evaluation.  It checks:

\begin{itemize}
  \item \textbf{Required fields} present (\texttt{id}, \texttt{name},
        \texttt{apiVersion}, \texttt{flow}).
  \item \textbf{Field types} correct (string vs.\ integer vs.\ object vs.\ array).
  \item \textbf{Enum values} valid (e.g.\ \texttt{kind} must be one of the
        four defined values).
  \item \textbf{Pattern constraints} satisfied (e.g.\ \texttt{id} must match
        slug pattern; \texttt{timeout} must match \texttt{[0-9]+(s|m|h)}).
  \item \textbf{minItems} / \textbf{minLength} constraints met
        (e.g.\ \texttt{flow} must have at least one node;
        \texttt{choices.options} must have at least two options).
  \item \textbf{additionalProperties} — no unknown core fields present.
\end{itemize}

Structural validation is performed by the generated JSON~Schema artifact
(see \S\ref{sec:schema-artifacts}).  It is stateless: it does not load tool
definitions, resolve imports, or evaluate templates.

\paragraph{Implementation note for Brian}

The Go runtime uses the \texttt{invopop/jsonschema} library to generate the
JSON~Schema from struct tags at startup (no static file required), then validates
the parsed YAML document against it.  The \texttt{yaml.v3 KnownFields(true)}
call provides an additional pre-JSON-Schema pass that rejects unknown fields
before the struct is even unmarshalled.

\subsection{Phase 2: Semantic Validation}
\label{subsec:semantic}

Semantic validation enforces domain rules that cannot be expressed in
JSON~Schema.  It runs after structural validation and requires the full runbook
AST and loaded tool/provider definitions.  Semantic checks include:

\begin{enumerate}
  \item \textbf{Step ID uniqueness} — All step IDs in the flat walk of the tree
        must be unique within the runbook.
  \item \textbf{Include target resolution} — Every \texttt{include.runbook} value
        must resolve to a relative file path that exists on disk.
  \item \textbf{Branch condition variable references} — Each \texttt{condition}
        template references only variables that are in scope at that point in the
        execution tree (declared as \texttt{inputs}, \texttt{vars}, or captured
        by an earlier step on a reachable path).
  \item \textbf{Tool reference resolution} — Every \texttt{tool.name} in a tool
        step must match a declared \texttt{toolRefs} entry, and the named
        \texttt{.tool.yaml} file must be loadable and structurally valid.
  \item \textbf{Tool action existence} — The \texttt{tool.action} must be
        declared in the referenced tool definition.
  \item \textbf{Provider binding resolution} — Every \texttt{inputs.*.from:
        <provider>.<field>} must resolve to a loaded provider whose field
        manifest declares that field name.
  \item \textbf{Cyclic include detection} — The include graph must be a DAG;
        any cycle is a semantic error reported as \texttt{cyclic include detected: A → B → A}.
  \item \textbf{Capture write-before-read} — A step that reads a captured
        variable via a template must have a reachable predecessor step that
        writes it; a warning is emitted if the write is only on some branches.
  \item \textbf{Governance policy pre-check} — The static governance rules are
        evaluated against the step contracts declared in the runbook.  Any
        step with an effect that would trigger \texttt{deny} is flagged as a
        semantic error.  Steps that would trigger \texttt{require-approval} are
        flagged as warnings.
  \item \textbf{Output declaration completeness} — Every declared \texttt{output}
        must reference a capture name that is written by at least one step.
  \item \textbf{Compensate ordering} — Each \texttt{compensate} step must appear
        after the step it is compensating for in tree walk order.
\end{enumerate}

\paragraph{Error reporting}

Both validation phases produce structured error reports with:
\begin{itemize}
  \item Error code (e.g.\ \texttt{SCH-001}, \texttt{SEM-007})
  \item File path and YAML line/column (where available)
  \item Human-readable message
  \item Suggested fix (where computable)
\end{itemize}

\begin{verbatim}
$ gert validate service-health.runbook.yaml
[SEM-003] step "check_http": tool action "head" not declared in
          tools/curl.tool.yaml (declared actions: get, post, download)
          --> service-health.runbook.yaml:34:7
[SEM-007] step "eval": template references variable "dns_result" which
          is only written on branch "DNS check" — may be unbound
          --> service-health.runbook.yaml:51:12
          hint: add a default value in vars: or guard with {{ if .dns_result }}
\end{verbatim}

% ============================================================
\section{Schema Artifacts}
\label{sec:schema-artifacts}
% ============================================================

\subsection{Shipped Artifacts}

gert v2 ships the following schema artifacts:

\begin{center}
\begin{tabular}{lp{9cm}}
\hline
\textbf{Artifact} & \textbf{Description} \\
\hline
\texttt{runbook.v2.schema.json}   & JSON~Schema Draft~2020-12 for runbook documents \\
\texttt{tool.v2.schema.json}      & JSON~Schema for tool definitions \\
\texttt{provider.v2.schema.json}  & JSON~Schema for provider definitions \\
\hline
\end{tabular}
\end{center}

All three schemas are generated at runtime from Go struct tags via
\texttt{invopop/jsonschema} and served by \texttt{gert schema export}.
They are also embedded in the binary and served at
\texttt{https://schemas.gert.dev/*} for IDE tooling.

\subsection{\texttt{gert schema export}}

\begin{minted}{bash}
# Export runbook schema (default)
gert schema export

# Export specific schema
gert schema export --kind runbook   # runbook | tool | provider | bundle

# Export full bundle (all three)
gert schema export --kind bundle > gert-v2-schemas.json

# Validate a document against the exported schema
gert schema export | check-jsonschema --schemafile - runbook.yaml
\end{minted}

The \texttt{bundle} format is a JSON object with three keys:
\texttt{runbook}, \texttt{tool}, and \texttt{provider}.

\subsection{VS Code Integration}

The gert VS~Code extension configures schema-backed YAML validation via the
\texttt{yaml-language-server} extension:

\begin{verbatim}
# .vscode/settings.json (auto-generated by gert vscode init)
{
  "yaml.schemas": {
    "https://schemas.gert.dev/runbook/v2.json": "**/*.runbook.yaml",
    "https://schemas.gert.dev/tool/v2.json":    "**/*.tool.yaml",
    "https://schemas.gert.dev/provider/v2.json": "**/*.provider.yaml"
  }
}
\end{verbatim}

Because the schema URL matches the \texttt{\$schema} field embedded in each
document, IDEs with built-in JSON~Schema support (VS~Code 1.80+, IntelliJ 2024+)
pick up validation and auto-completion without any per-project configuration.

The gert VS~Code extension additionally provides:
\begin{itemize}
  \item Step-type-aware field completion via the \texttt{schema/stepFields}
        JSON-RPC method (returns the field set for a given \texttt{type} value).
  \item Tool argument completion via \texttt{schema/toolArgs} (returns the
        \texttt{args} schema for a given tool+action pair from the live tool
        catalog).
  \item Inline governance warnings derived from \texttt{exec/dryRun} pre-check.
\end{itemize}

\subsection{Schema Version Manifest}

Each schema artifact carries a version manifest in its \texttt{\$id} and a
\texttt{x-gert-version} annotation:

\begin{minted}{json}
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.gert.dev/runbook/v2.json",
  "x-gert-version": "2.0.0",
  "x-gert-released": "2026-04-18",
  "title": "gert runbook v2",
  ...
}
\end{minted}

Automated compatibility testing (part of the gert CI pipeline) runs every
v1 example runbook through the v2 compatibility parser and asserts no regression,
and runs every v2 example through the exported JSON~Schema validator.
