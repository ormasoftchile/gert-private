# Orchestration Log — Don

**Session:** 2026-08-16T22:59:48Z  
**Role:** Backend Dev (background)  
**Task:** Ground-truth verification of all inbound ask claims against Gert Go source code

## Deliverable

`.squad/decisions/archive/don-runtime-portability-ground-truth-2026-08-16.md`

## Summary

Performed comprehensive source-code audit of the ask's 13 claims (8 "exists", 5 "does not exist"). Verdict: accurate on all substantive points except `run-gert.ps1` (does not exist in Gert core; may be in consumer repo). All infrastructure for ask's core ask already present: mcp-http transport, AuthProvider interface, TokenGate policy enforcement, AzureCLIAuthProvider, all with production semantics.

## Key findings

1. **7/8 "exists" claims TRUE** — only `run-gert.ps1` unverified (not in source).
2. **All 5 "does not exist" claims confirmed absent** — runtime profile, transport override mechanism, VS Code bridge, managed identity, workload identity.
3. **Surprise finding:** `ToolGovernance.AllowedEnvironments []string` and `RequiresCapabilities []string` declared in schema but completely unenforced in Go code (zero references). This is immediate quick-win for Cristiano's core concern (preflight capability check).
4. **Phase estimates critique:** Phase 1 optimistic (6–8wk, not 4–6); Phase 2 wildly optimistic without OQ-2 answer; Phase 3 underestimates idempotency/reconnect scope.

## Attendant work

- Wire existing `AllowedEnvironments` field into planner's tool-resolution path (low-cost preflight improvement).
- Revisit Phase 1 and Phase 2 estimates after OQ-2 resolution.
