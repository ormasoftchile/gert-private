# Orchestration Log — David (Integration Engineer)
**Date:** 2026-08-17T18:13:01Z
**Task:** Phase 1B Rev 2 Corrections (Fail-Fast, Indeterminate, ICM Contract)

## Work Completed

### Correction A — Profileless Non-Interactive Fail-Fast

- **Implementation:** CLI entry in `cmd/gert/run.go` after profile check.
- **Detection method:** `os.Stdin.Stat()` + `ModeCharDevice` (stdlib only, no new dependency).
- **Placement:** Before `BuildEngineConfig`, gates both `TerminalApprovalGate` installation and prompt provider.
- **Error message:** "non-interactive execution requires an unattended runtime profile"
- **Exit code:** `exitValidation` (exit 1).

#### Harness Impact
- **Finding:** Conformance harness breaks without profile.
- **Root cause:** `enum_harness.go:runCLI()` invokes `gert run` without TTY; will hit fail-fast.
- **Solution:** Supply minimal unattended test profile fixture at `internal/conformance/testdata/unattended-test.profile.yaml`.
- **Result:** Behavior unchanged (already using `NoOpApprovalGate`); fail-fast satisfied at CLI boundary.

**Estimate:** 1.5 days total (implementation + harness + regression pass).

### Correction B — INDETERMINATE Must Retain Trace-Safe Evidence

#### Seven Missing Evidence Fields
The current `StepResult` struct lacks:
1. Logical tool name
2. Logical action name
3. Classification
4. Endpoint host
5. Attempt number
6. Deadline (context deadline)
7. Transport error category

#### Non-Fabrication Representation
- **Design:** Embed `*IndeterminateRecord` pointer on `StepResult`.
- **Signal:** Non-nil pointer = completion unknown; `Output` remains nil (no fabrication).
- **Redaction:** Host only (not full URL), never tokens or credentials.

#### IndeterminateRecord Structure
```
RunID, StepID, ToolName, ActionName, Classification, 
EndpointHost, AttemptNumber, Deadline, FailureTime, 
TransportErrCategory
```

**Estimate:** 5.5–6 days (struct definition, recording site, resume guard, test assertions).

### Correction C — ICM Proof Must Target Real Contract

#### Package-Map and Profile Status
- **`--package-map`:** YES — execution-wired. Proven by `TestRun_PackageMap_RealVsMockBinding`.
- **`--profile`:** NOT execution-wired yet. This is Phase 1B Item 2 work.

#### Contract Parity Assertion Infrastructure
- **Gap:** No infrastructure exists to compare outputs across two bindings of the same logical tool.
- **Requirement:** Mock server (httptest or stdio) that returns contract-identical outputs.
- **Estimate:** 1 day to build parity harness.

#### Blocking Artifacts from SQL Live-Site Operations
Six artifacts required; Item 4 cannot start until all are received:
1. Full `icm-tsg-router.runbook.yaml` content
2. `icm.get-incident` tool definition with hyphen, full schema (title, service, environment, logical_server, database)
3. `tsg-recommendation.recommend` tool definition with both outcome shapes
4. Their `--package-map` file or equivalent config
5. Production transport mode declaration (mcp-stdio vs mcp-http)
6. Whether runbook uses `toolRefs:` with package names or bare references

#### Item 4 Revised Estimate
- Mock server + parity assertions: 1 day
- Cross-binding test infrastructure: 1 day
- Profile-at-execution wiring (depends on Item 2): included in Item 2
- Recommendation outcomes: 0.5 day
- Integration test: 0.5 day
- **Total: 3 days** (after Item 2 ships AND artifacts received).

## Conclusion

Fail-fast straightforward; harness opt-in required. INDETERMINATE evidence record adds 1.5 days to original estimate. ICM proof gated on Item 2 completion and external artifact delivery; revised to 3 days with contract-correct scope.
