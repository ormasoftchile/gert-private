# Orchestration Log: hisoka-bc-fix

**Timestamp:** 2026-04-06T10:09:48Z  
**Agent:** Hisoka (QA Lead)  
**Task:** Fix remaining template variables and `<no value>` escaping  
**Status:** ✅ Complete

## Dispatch

Hisoka was tasked to identify remaining template variable handling and `<no value>` issues.

## Analysis

- `primary_host` is a prompt input without default value
- `ResolveTemplatePublic` with `missingkey=zero` renders missing vars as `<no value>`
- Comment on ResolveTemplatePublic indicated intent to strip this but only checked full string
- Need to filter substrings throughout template rendering

## Work

1. Updated `ResolveTemplatePublic` in `pkg/engine/engine.go` to filter `<no value>` from all results
2. Frontend sanitizers handle remaining unresolvable variables
3. Combined backend + frontend approach provides defense-in-depth

## Commits

- 5eca8a8 — fix: resolve step titles + strip `<no value>`

## Outcomes

- Backend filtering prevents `<no value>` from reaching frontend
- Frontend sanitizers handle any edge cases
- Combined approach robust to future template edge cases
- All tests passing
